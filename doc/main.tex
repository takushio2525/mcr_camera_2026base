%% ============================================================
%% mcr_camera_2026base プログラム設計仕様書
%% ============================================================
%% ビルド: latexmk main.tex (.latexmkrc で uplatex + dvipdfmx)
%% ============================================================
\documentclass[a4paper,11pt,dvipdfmx]{jsarticle}

%% ─── パッケージ ───────────────────────────────
\usepackage[dvipdfmx]{graphicx}
\usepackage[dvipdfmx]{xcolor}
\usepackage[dvipdfmx,
  bookmarks=true,
  bookmarksnumbered=true,
  colorlinks=true,
  linkcolor={blue!70!black},
  citecolor={green!50!black},
  urlcolor={blue!60!black},
  pdfauthor={Shiozawa},
  pdftitle={mcr\_camera\_2026base プログラム設計仕様書},
  pdfsubject={マイコンカーラリー カメラクラス開発仕様書},
]{hyperref}
\usepackage{pxjahyper}           % hyperref 日本語しおり対応
\usepackage[top=25mm,bottom=25mm,left=20mm,right=20mm]{geometry}
\usepackage{fancyhdr}
\usepackage{lastpage}
\usepackage{tabularx}
\usepackage{booktabs}
\usepackage{longtable}
\usepackage{enumitem}
\usepackage{titlesec}
\usepackage{tcolorbox}
\tcbuselibrary{skins,breakable}
\usepackage{tikz}
\usetikzlibrary{arrows.meta,positioning,shapes.geometric,fit,calc}
\usepackage{listings}
\usepackage{caption}
\usepackage{float}
\usepackage{amsmath}

%% ─── 色定義 ──────────────────────────────────
\definecolor{headerblue}{HTML}{1B3A5C}
\definecolor{accentblue}{HTML}{2E86C1}
\definecolor{lightgray}{HTML}{F2F3F4}
\definecolor{codebg}{HTML}{F7F9FB}
\definecolor{warnred}{HTML}{E74C3C}
\definecolor{noteyellow}{HTML}{F39C12}
\definecolor{okgreen}{HTML}{27AE60}

%% ─── ヘッダー・フッター ───────────────────────
\pagestyle{fancy}
\fancyhf{}
\fancyhead[L]{\small\color{headerblue}\textbf{mcr\_camera\_2026base} プログラム設計仕様書}
\fancyhead[R]{\small\color{headerblue}Rev.\,2.0}
\fancyfoot[C]{\small\thepage\,/\,\pageref{LastPage}}
\fancyfoot[R]{\small\color{gray}2026-08-13}
\renewcommand{\headrulewidth}{0.8pt}
\renewcommand{\footrulewidth}{0.4pt}

%% ─── セクション見出しスタイル ─────────────────
\titleformat{\section}
  {\Large\bfseries\color{headerblue}}
  {\thesection}{1em}{}
  [\vspace{-0.5em}\textcolor{accentblue}{\rule{\textwidth}{1.2pt}}]
\titleformat{\subsection}
  {\large\bfseries\color{headerblue}}
  {\thesubsection}{1em}{}
\titleformat{\subsubsection}
  {\normalsize\bfseries\color{accentblue}}
  {\thesubsubsection}{1em}{}

%% ─── listings 設定 (C++) ──────────────────────
\lstdefinestyle{cppstyle}{
  language=C++,
  basicstyle=\ttfamily\small,
  keywordstyle=\color{accentblue}\bfseries,
  commentstyle=\color{gray}\itshape,
  stringstyle=\color{okgreen},
  numberstyle=\tiny\color{gray},
  numbers=left,
  numbersep=8pt,
  frame=single,
  framerule=0.5pt,
  rulecolor=\color{accentblue!40},
  backgroundcolor=\color{codebg},
  breaklines=true,
  breakatwhitespace=false,
  tabsize=2,
  showstringspaces=false,
  captionpos=b,
  xleftmargin=15pt,
  framexleftmargin=15pt,
  morekeywords={override,float,int,void,static,const},
}
\lstset{style=cppstyle}

%% ─── tcolorbox スタイル ──────────────────────
% データシート風ボックス
\newtcolorbox{apibox}[1]{
  enhanced,
  colback=white,
  colframe=accentblue,
  coltitle=white,
  fonttitle=\bfseries\large,
  title=#1,
  top=4mm,
  arc=2mm,
  breakable,
  before upper={\parindent0pt},
  attach boxed title to top left={yshift=-3mm,xshift=5mm},
  boxed title style={colback=accentblue,arc=1.5mm},
}

% 注意ボックス
\newtcolorbox{cautionbox}[1][注意]{
  enhanced,
  colback=warnred!5,
  colframe=warnred!70,
  coltitle=white,
  fonttitle=\bfseries,
  title={\raisebox{-0.1em}{\textbf{⚠}} #1},
  arc=1.5mm,
  breakable,
  attach boxed title to top left={yshift=-2mm,xshift=5mm},
  boxed title style={colback=warnred!70,arc=1mm},
}

% ノートボックス
\newtcolorbox{notebox}[1][補足]{
  enhanced,
  colback=accentblue!5,
  colframe=accentblue!50,
  coltitle=white,
  fonttitle=\bfseries,
  title={$\triangleright$ #1},
  arc=1.5mm,
  breakable,
  attach boxed title to top left={yshift=-2mm,xshift=5mm},
  boxed title style={colback=accentblue!50,arc=1mm},
}

% 関数ドキュメント用ボックス (FSPスタイル)
\newtcolorbox{funcblock}[1]{
  enhanced,
  colback=white,
  colframe=accentblue!25,
  coltitle=headerblue,
  fonttitle=\bfseries\ttfamily\small,
  title={◆ #1},
  arc=1mm,
  boxrule=0.4pt,
  breakable,
  left=3mm, right=3mm, top=2mm, bottom=2mm,
  before skip=3mm, after skip=3mm,
  before upper={\parindent0pt},
}

% APIセクション見出し (FSPスタイル)
\newcommand{\apisection}[1]{%
  \vspace{4mm}%
  {\normalsize\bfseries\color{headerblue} #1}%
  \par\vspace{-1mm}%
  {\color{accentblue!40}\rule{\linewidth}{0.5pt}}%
  \par\vspace{2mm}%
}

% 実行フェーズバッジ
\newcommand{\phasebadge}[1]{%
  \colorbox{accentblue!10}{\small\bfseries\color{accentblue!80!black}\,#1\,}%
}

%% ─── ドキュメント情報 ─────────────────────────
\newcommand{\doctitle}{mcr\_camera\_2026base\\プログラム設計仕様書}
\newcommand{\docsubtitle}{GR-PEACH (RZ/A1H) マイコンカーラリー カメラクラス開発}
\newcommand{\docauthor}{Shiozawa}
\newcommand{\docversion}{2.0}
\newcommand{\docdate}{2026年8月13日}

%% ============================================================
\begin{document}

%% ─── 表紙 ─────────────────────────────────────
\begin{titlepage}
\begin{center}
\vspace*{30mm}

{\color{accentblue}\rule{\textwidth}{2pt}}
\vspace{8mm}

{\Huge\bfseries\color{headerblue} \doctitle}

\vspace{6mm}

{\Large\color{accentblue} \docsubtitle}

\vspace{4mm}
{\color{accentblue}\rule{\textwidth}{2pt}}

\vspace{20mm}

\begin{tabular}{rl}
  \textbf{バージョン} & \docversion \\[4pt]
  \textbf{作成日}     & \docdate \\[4pt]
  \textbf{作成者}     & \docauthor \\[4pt]
  \textbf{ステータス} & ドラフト \\
\end{tabular}

\vfill

\begin{tcolorbox}[
  colback=lightgray,
  colframe=headerblue,
  width=0.8\textwidth,
  arc=3mm,
  halign=center,
]
\small
本文書は \texttt{mcr\_camera\_2026base} プロジェクトのプログラム設計仕様書です。\\
ベアメタル環境における割り込み駆動アーキテクチャの詳細設計を記述します。
\end{tcolorbox}

\vspace{10mm}
{\small\color{gray} 機密区分: 内部利用}

\end{center}
\end{titlepage}

%% ─── 改訂履歴 ─────────────────────────────────
\newpage
\section*{改訂履歴}
\addcontentsline{toc}{section}{改訂履歴}

\begin{tabularx}{\textwidth}{c c c X}
\toprule
\textbf{版} & \textbf{日付} & \textbf{著者} & \textbf{変更内容} \\
\midrule
1.0  & 2026-02-19 & Shiozawa & 初版作成 \\
1.1  & 2026-02-24 & Shiozawa & Onboardクラス・1msタイマーの実装 \\
1.2  & 2026-02-24 & Shiozawa & オンボードクラスのインスタンス化・GPIO初期化修正 \\
1.3  & 2026-02-24 & Shiozawa & GIC割り込みディスパッチャ（IRQ）の実装 \\
1.4  & 2026-02-24 & Shiozawa & CPUベクタベースアドレス(VBAR)の初期化追加 \\
1.5  & 2026-02-24 & Shiozawa & 割り込み動作の根本原因（Cortex-A9）修正 \\
1.6  & 2026-02-24 & Shiozawa & OSTMタイマー周期がハードに反映されない問題の修正 \\
1.7  & 2026-02-24 & Shiozawa & タイマー割り込みが1回しか発生しない不具合の修正 \\
1.8  & 2026-02-24 & Shiozawa & 個別割り込みハンドラからの不正リターン命令修正 \\
1.9  & 2026-02-24 & Shiozawa & IModuleインターフェース実装・Onboardグローバル化 \\
1.10 & 2026-02-24 & Shiozawa & OnboardのsetLedをsetUserLed/setColorLedに分割 \\
1.11 & 2026-02-24 & Shiozawa & シリアル通信（Serial）クラスの実装 \\
1.12 & 2026-02-26 & Shiozawa & LaTeXコンパイル用DevContainer環境の統合 \\
1.13 & 2026-03-06 & Shiozawa & SPEC.mdの内容をTeXに統合・README.md新規作成 \\
1.14 & 2026-05-03 & Shiozawa & RunController クラス（メイン走行制御）追加。エンコーダ距離制御をタイマー（ms単位）に置換。 \\
2.0  & 2026-08-13 & Shiozawa & \textbf{実装準拠への全面改訂。}EMA (Embedded-Module-Architecture) 化を反映し、
  \texttt{IModule} を \texttt{updateInput}/\texttt{updateOutput} 版に更新。
  \texttt{SystemData} / \texttt{ProjectConfig} / \texttt{ModuleTimer} を新規記述。
  実在しない Trace 章を LineDetector 章へ、RunController 章を RunLogic 章へ差し替え。
  LineDetector / SDLogger / 起動シーケンス / パラメータ調整ガイドを追加。
  \texttt{mcr\_camera\_2026base\_SPEC.md} の技術知見を「ベアメタル固有の実装知見」章と
  付録「不具合と対策の記録」へ移植し、同ファイルを廃止。 \\
\bottomrule
\end{tabularx}

%% ─── 目次 ─────────────────────────────────────
\newpage
\tableofcontents
\newpage

%% ============================================================
%% 1. 概要
%% ============================================================
\section{概要}\label{sec:overview}

GR-PEACH (RZ/A1H) を使用した、マイコンカーラリー (MCR) カメラクラス開発のためのベースプログラムである。

\begin{notebox}[設計思想]
\begin{itemize}[nosep]
  \item mbed OS を使用せず、ルネサス標準の \texttt{iodefine.h} を用いたベアメタル（レジスタ直接操作）環境で構築する
  \item 既存のスパゲッティコードを廃し、EMA (Embedded-Module-Architecture) 準拠のモジュール分割を採用する。
        全モジュールは共通インターフェース \texttt{IModule} を実装し、
        \texttt{init()} / \texttt{updateInput()} / \texttt{updateOutput()} の3点で統一的に扱う
  \item モジュール間のデータ受け渡しは共有構造体 \texttt{SystemData} に一本化し、
        モジュールが他モジュールのメソッドを直接呼ぶことを禁止する
  \item 調整対象の定数は \texttt{src/core/ProjectConfig.h} に集約し、
        「走行調整のためにドライバのソースを触る」状況をなくす
\end{itemize}
\end{notebox}

\subsection{用語定義}

\begin{tabularx}{\textwidth}{l X}
\toprule
\textbf{用語} & \textbf{定義} \\
\midrule
MCR       & マイコンカーラリーの略称 \\
GR-PEACH  & ルネサス RZ/A1H 搭載の開発ボード \\
MTU2      & マルチファンクション・タイマ・ユニット2。PWM出力・位相計数に使用 \\
VDC5      & ビデオディスプレイコントローラ5。DVDEC と組み合わせて NTSC 映像を取り込む \\
EMA       & Embedded-Module-Architecture。\texttt{IModule} / \texttt{SystemData} /
            \texttt{\{Module\}Config} の3点セットでモジュールを構成する設計方針 \\
IModule   & 全モジュール共通の基底インターフェース (\hyperref[sec:api-imodule]{§\ref*{sec:api-imodule}}) \\
SystemData & 全モジュールの入出力データを集約する共有構造体 (\hyperref[sec:systemdata]{§\ref*{sec:systemdata}}) \\
Config    & モジュールごとの設定構造体。実値は \texttt{ProjectConfig.h} で一括定義する
            (\hyperref[sec:params]{§\ref*{sec:params}}) \\
ModuleTimer & 1ms カウンタ \texttt{g\_timer\_1ms} を時刻源とするノンブロッキングタイマー
            (\hyperref[sec:moduletimer]{§\ref*{sec:moduletimer}}) \\
ラッチ方式 & 出力値をいったん内部バッファに保持し、出力フェーズでのみハードウェアへ一括反映する方式 \\
XIP       & eXecute In Place。SPI フラッシュ上のコードを RAM へ展開せず直接実行する方式
            (\hyperref[sec:xip]{§\ref*{sec:xip}}) \\
\bottomrule
\end{tabularx}

%% ============================================================
%% 2. 背景・課題
%% ============================================================
\section{背景・課題}\label{sec:background}

\begin{cautionbox}[開発環境の課題]
\begin{enumerate}[nosep]
  \item \textbf{mbed OS サービス終了}: mbed のサービス終了に伴い、将来的な継続利用や保守が困難になる
  \item \textbf{開発環境の老朽化}: 既存プロジェクトの e2 studio 環境やコンパイラバージョンが古く、最新の環境でビルドを通す必要がある
  \item \textbf{ブラックボックス化の解消}: OS内部処理への依存を減らし、デバッグを容易にする必要がある
\end{enumerate}
\end{cautionbox}

%% ============================================================
%% 3. 目的
%% ============================================================
\section{目的}\label{sec:purpose}

\begin{enumerate}[nosep]
  \item mbed OS に依存しない、RZ/A1H 用の C++ ベースプログラム環境の構築
  \item \textbf{クラス化・モジュール化}による可読性・保守性の向上
  \item 共通インターフェース (\texttt{IModule}) による統一的な制御フローの確立
\end{enumerate}

%% ============================================================
%% 4. システムアーキテクチャ
%% ============================================================
\section{システムアーキテクチャ}\label{sec:architecture}

システムは OSTM0 による 1ms 周期のタイマー割り込みで同期して動作する。
割り込みハンドラ \texttt{ostm0\_interrupt\_callback()} が
Input $\rightarrow$ Logic $\rightarrow$ Output の3フェーズを毎周期実行し、
メインループはフレーム完了待ち・ライン検出・ログ処理・デバッグ表示を担当する。

\subsection{実行フロー図}\label{sec:flow}

\begin{figure}[H]
\centering
\begin{tikzpicture}[
  node distance=6mm and 10mm,
  phase/.style={
    draw=accentblue, fill=accentblue!10,
    rounded corners=3pt, minimum width=52mm, minimum height=10mm,
    font=\bfseries\small, align=center,
  },
  module/.style={
    draw=headerblue, fill=headerblue!8,
    rounded corners=2pt, minimum width=52mm, minimum height=8mm,
    font=\small\ttfamily, align=center,
  },
  hub/.style={
    draw=okgreen, fill=okgreen!8,
    rounded corners=2pt, minimum width=22mm, minimum height=44mm,
    font=\small\ttfamily, align=center,
  },
  arr/.style={-{Stealth[length=3mm]}, thick, color=accentblue},
  arrg/.style={-{Stealth[length=3mm]}, thick, color=okgreen!80!black},
]

% フェーズ
\node[phase] (input) {Input フェーズ};
\node[phase, below=17mm of input] (logic) {Logic フェーズ};
\node[phase, below=17mm of logic] (output) {Output フェーズ};

% Input モジュール
\node[module, right=32mm of input, yshift=5mm]  (onbi) {g\_onboard.updateInput()};
\node[module, right=32mm of input, yshift=-5mm] (cam)  {g\_camera.updateInput()};

% Logic
\node[module, right=32mm of logic] (pat) {applyDrivingPattern()};

% Output モジュール
\node[module, right=32mm of output, yshift=10mm] (mot) {g\_motor.updateOutput()};
\node[module, right=32mm of output, yshift=0mm]  (srv) {g\_servo.updateOutput()};
\node[module, right=32mm of output, yshift=-10mm](onbo) {g\_onboard.updateOutput()};

% SystemData ハブ
\node[hub, left=14mm of logic] (sys) {g\_sys\\(SystemData)};

% 矢印（フェーズの進行）
\draw[arr] (input) -- (logic);
\draw[arr] (logic) -- (output);

\draw[arr] (input.east) ++(0,5mm) -- (onbi.west);
\draw[arr] (input.east) ++(0,-5mm) -- (cam.west);
\draw[arr] (logic.east) -- (pat.west);
\draw[arr] (output.east) ++(0,10mm) -- (mot.west);
\draw[arr] (output.east) -- (srv.west);
\draw[arr] (output.east) ++(0,-10mm) -- (onbo.west);

% SystemData との読み書き
\draw[arrg] (input.west)  -| node[font=\scriptsize\color{gray}, above, pos=0.25]{書込} (sys.north);
\draw[arrg] (sys.east)    -- node[font=\scriptsize\color{gray}, above]{読書} (logic.west);
\draw[arrg] (sys.south)   |- node[font=\scriptsize\color{gray}, below, pos=0.75]{読出} (output.west);

\end{tikzpicture}
\caption{1ms 割り込みハンドラの3フェーズ実行フロー}
\label{fig:flow}
\end{figure}

Input / Output の各フェーズは \texttt{IModule*} の配列 (\texttt{s\_inputModules[]} /
\texttt{s\_outputModules[]}) をループして呼び出す。
モジュールの \texttt{enabled} が \texttt{false} の間はスキップされるため、
初期化に失敗したモジュールを配列から外さずに無効化できる。

\begin{lstlisting}[caption={Input / Output フェーズの配列駆動 (mcr\_camera\_2026base.cpp)},label=lst:phasearray]
static IModule* s_inputModules[] = {
    &g_onboard,   // Input : SW 状態を sys.ob.sw へ
    &g_camera,    // Input : 1ms ごとのフレーム段階処理
};
static IModule* s_outputModules[] = {
    &g_motor,     // Output : sys.mot.*Cmd  -> PWM
    &g_servo,     // Output : sys.srv.angleCmd -> PWM
    &g_onboard,   // Output : sys.ob.*LedCmd  -> GPIO
};

for (int i = 0; i < N_IN; ++i) {
    if (s_inputModules[i]->enabled) s_inputModules[i]->updateInput(g_sys);
}
\end{lstlisting}

\subsection{3フェーズモデル}\label{sec:threephase}

\begin{tabularx}{\textwidth}{c l X}
\toprule
\textbf{順序} & \textbf{フェーズ} & \textbf{内容} \\
\midrule
1 & Input  & ハードウェア $\rightarrow$ \texttt{SystemData}。各モジュールの
             \texttt{updateInput(sys)} がセンサ値を \texttt{sys.*} へ書き込む \\
2 & Logic  & \texttt{SystemData} $\rightarrow$ \texttt{SystemData}。
             \texttt{applyDrivingPattern(sys)} が状態機械を進め、
             出力指示を \texttt{sys.mot.*Cmd} / \texttt{sys.srv.angleCmd} /
             \texttt{sys.ob.*LedCmd} へ\textbf{代入する}（\textbf{ドライバは呼ばない}） \\
3 & Output & \texttt{SystemData} $\rightarrow$ ハードウェア。各モジュールの
             \texttt{updateOutput(sys)} が \texttt{*Cmd} を読み、レジスタへ反映する \\
\bottomrule
\end{tabularx}

\begin{notebox}[3フェーズ分離の利点]
Logic フェーズはハードウェアに一切触れない純粋な計算になるため、
\texttt{SystemData} を差し替えるだけで PC 上の単体テストに載せられる。
また出力タイミングが Output フェーズに集約されるので、
1ms 内で出力が二度書きされたり、Logic の途中経過が物理出力に漏れることがない。
\end{notebox}

\begin{cautionbox}[全モジュールがラッチ方式ではない]
Output フェーズ内での反映方法はモジュールによって異なる。
\texttt{Onboard} は4個の LED 状態を内部バッファ \texttt{ledState\_[]} にまとめてから
GPIO へ一括書き込みする真のラッチ方式だが、
\texttt{Motor} / \texttt{Servo} は \texttt{updateOutput()} 内で MTU2 レジスタへ直接書き込む。
いずれも「Output フェーズでのみ書く」点は共通であり、
Logic フェーズの途中経過が出力に漏れない性質は保たれている。
\end{cautionbox}

\subsection{共有データ構造 SystemData}\label{sec:systemdata}

\texttt{SystemData}（\texttt{src/core/SystemData.h}）は全モジュールの入出力データを
1つに集約した構造体である。実体はグローバル変数 \texttt{g\_sys}
（\texttt{mcr\_camera\_2026base.cpp} で定義）1個のみで、
3フェーズの全呼び出しがこの参照を受け取る。

\begin{lstlisting}[caption={SystemData の定義},label=lst:systemdata]
struct SystemData {
    CameraData       cam;   // Camera       が所有
    LineDetectorData line;  // LineDetector が所有
    MotorData        mot;   // Motor        が所有
    ServoData        srv;   // Servo        が所有
    OnboardData      ob;    // Onboard      が所有
    EncoderData      enc;   // Encoder      が所有
    SerialData       ser;   // Serial       が所有
    SDLoggerData     sd;    // SDLogger     が所有
    RunData          run;   // RunLogic     が所有
};
extern SystemData g_sys;
\end{lstlisting}

\begin{tabularx}{\textwidth}{l l X}
\toprule
\textbf{メンバ} & \textbf{型} & \textbf{主な内容} \\
\midrule
\texttt{cam}  & \texttt{CameraData}       & \texttt{frameReady}, \texttt{frameCount}, \texttt{field}, \texttt{imageBufferPtr} \\
\texttt{line} & \texttt{LineDetectorData} & \texttt{deviation[120]}, \texttt{crossLine}, \texttt{leftLine}, \texttt{rightLine}, \texttt{centerLine}, \texttt{sensorBin} \\
\texttt{mot}  & \texttt{MotorData}        & \texttt{leftCmd}, \texttt{rightCmd}, \texttt{leftActual}, \texttt{rightActual} \\
\texttt{srv}  & \texttt{ServoData}        & \texttt{angleCmd}, \texttt{angleActual} \\
\texttt{ob}   & \texttt{OnboardData}      & \texttt{sw}, \texttt{ledRedCmd}, \texttt{ledGreenCmd}, \texttt{ledBlueCmd}, \texttt{userLedCmd} \\
\texttt{enc}  & \texttt{EncoderData}      & \texttt{totalCount}, \texttt{magaCount}, \texttt{cnt}, \texttt{avgCnt}, \texttt{filteredCnt} \\
\texttt{ser}  & \texttt{SerialData}       & \texttt{txCount} \\
\texttt{sd}   & \texttt{SDLoggerData}     & \texttt{ready}, \texttt{logCount}, \texttt{full}, \texttt{saveRequested}, \texttt{saveDone} \\
\texttt{run}  & \texttt{RunData}          & \texttt{pattern}, \texttt{handleVal}, \texttt{finished}, 3本の \texttt{ModuleTimer} \\
\bottomrule
\end{tabularx}

\begin{notebox}[データの所有権]
\texttt{\{Module\}Data} 構造体の宣言は各モジュールのヘッダ側に置き、
\texttt{SystemData.h} はそれを include して集約するだけである。
各モジュールは原則として\textbf{自身の Data だけ}を読み書きし、
他モジュールのデータを横断参照してよいのは Logic フェーズの関数だけ、というルールで運用する。
\end{notebox}

\begin{cautionbox}[画素バッファは SystemData に置かない]
160$\times$120 の輝度バッファ（約 19KB）と YCbCr422 中間バッファ（約 38KB）は
\texttt{Camera} クラス内に実体を持ち、\texttt{SystemData} からは
\texttt{cam.imageBufferPtr} 経由で参照する。
巨大な配列を \texttt{SystemData} に持たせるとメモリ効率とコピーコストが悪化するため。
\end{cautionbox}

\subsection{ModuleTimer --- ノンブロッキング時間計測}\label{sec:moduletimer}

ベアメタル環境には Arduino の \texttt{millis()} に相当する API がないため、
OSTM0 の 1ms 割り込みでインクリメントされるグローバルカウンタ
\texttt{volatile unsigned long g\_timer\_1ms} を時刻源とする軽量クラスを用意している
（\texttt{src/core/ModuleTimer.h}）。

\begin{tabularx}{\textwidth}{l X}
\toprule
\textbf{メソッド} & \textbf{機能} \\
\midrule
\texttt{void setTime()}                    & 現在時刻を起点としてタイマーを開始する \\
\texttt{unsigned long getNowTime() const}  & \texttt{setTime()} からの経過時間 [ms]。未起動なら 0 \\
\texttt{bool isExpired(unsigned long ms) const} & 指定時間以上経過したか。未起動なら \texttt{false} \\
\texttt{void reset()}                      & 停止状態に戻す \\
\texttt{bool isRunning() const}            & 起動中かどうか \\
\bottomrule
\end{tabularx}

\vspace{2mm}
\texttt{RunData} はこれを3本保持し、旧実装のカウンタ変数を置き換えている
(\hyperref[sec:api-runlogic]{§\ref*{sec:api-runlogic}})。

\begin{notebox}[待ちループを書かないこと]
\texttt{ModuleTimer} は「経過したか」を問い合わせるだけで、内部でブロックしない。
1ms 割り込みの中で \texttt{while (!timer.isExpired(n))} のような待機を書くと
\texttt{g\_timer\_1ms} が進まなくなり、確実にハングする。
\end{notebox}

\subsection{設定注入と ProjectConfig}\label{sec:configinject}

各モジュールは \texttt{\{Module\}Config} 構造体を\textbf{コンストラクタ引数で受け取り}、
内部に \texttt{\_config} としてコピーして保持する。
実値の定義は \texttt{src/core/ProjectConfig.h} の1ファイルに集約されており、
走行パラメータの調整でドライバのソースを編集する必要はない。

\begin{lstlisting}[caption={Config 注入によるインスタンス生成 (mcr\_camera\_2026base.cpp)},label=lst:configinject]
Serial       g_serial      (SERIAL_CONFIG);
Onboard      g_onboard     (ONBOARD_CONFIG);
Motor        g_motor       (MOTOR_CONFIG);
Servo        g_servo       (SERVO_CONFIG);
Encoder      g_encoder     (ENCODER_CONFIG);   // 現状 init/呼出は未接続
Camera       g_camera      (CAMERA_CONFIG);
LineDetector g_lineDetector(LINE_DETECTOR_CONFIG);
SDCard       g_sdcard      (SDCARD_CONFIG);
SDLogger     g_sdlogger    (SDLOGGER_CONFIG);
\end{lstlisting}

実際の設定値は \hyperref[sec:params]{§\ref*{sec:params}}「パラメータ調整ガイド」にまとめる。

\begin{cautionbox}[Config はグローバル ctor に依存する]
\texttt{\_config} メンバはコンストラクタ初期化で埋まる。
本プロジェクトの \texttt{start.S} は \texttt{\_\_libc\_init\_array()} を呼ばないため、
\texttt{main()} 冒頭の \texttt{runGlobalConstructors()} を経由しないと
\texttt{\_config} が BSS のゼロのままになり、サーボ出力が 0 になる等の致命的な不具合になる。
詳細は \hyperref[sec:ctors]{§\ref*{sec:ctors}} を参照。
\end{cautionbox}

%% ============================================================
%% 5. ファイル構成
%% ============================================================
\section{ファイル構成}\label{sec:files}

\subsection{ディレクトリ構造}

\begin{tcolorbox}[colback=codebg, colframe=accentblue!40, arc=2mm]
\ttfamily\small
\begin{tabbing}
\hspace{4mm}\=\hspace{4mm}\=\hspace{4mm}\=\hspace{4mm}\=\kill
mcr\_camera\_2026base/ \\
\> src/ \\
\>\> mcr\_camera\_2026base.cpp \quad\textrm{\color{gray}← メイン・GIC/OSTM0 初期化・割り込みハンドラ} \\
\>\> core/ \\
\>\>\> IModule.h \quad\textrm{\color{gray}← 基底インターフェース} \\
\>\>\> SystemData.h \quad\textrm{\color{gray}← 全モジュール共有データ} \\
\>\>\> ProjectConfig.h \quad\textrm{\color{gray}← 全パラメータの実値定義} \\
\>\>\> ModuleTimer.h \quad\textrm{\color{gray}← ノンブロッキングタイマー} \\
\>\> logic/ \\
\>\>\> RunLogic.h / RunLogic.cpp \quad\textrm{\color{gray}← 走行状態機械（純関数）} \\
\>\> drivers/ \\
\>\>\> Camera.h / Camera.cpp \quad\textrm{\color{gray}← NTSC キャプチャ (VDC5+DVDEC)} \\
\>\>\> LineDetector.h / .cpp \quad\textrm{\color{gray}← ライン検出・偏差算出} \\
\>\>\> Encoder.h / Encoder.cpp \quad\textrm{\color{gray}← エンコーダ制御} \\
\>\>\> Motor.h / Motor.cpp \quad\textrm{\color{gray}← モータ制御} \\
\>\>\> Servo.h / Servo.cpp \quad\textrm{\color{gray}← サーボ制御} \\
\>\>\> Onboard.h / Onboard.cpp \quad\textrm{\color{gray}← LED・スイッチ制御} \\
\>\>\> Serial.h / Serial.cpp \quad\textrm{\color{gray}← シリアル通信} \\
\>\>\> SDCard.h / SDCard.cpp \quad\textrm{\color{gray}← SD カード SPI ブロックドライバ} \\
\>\>\> SDLogger.h / SDLogger.cpp \quad\textrm{\color{gray}← ログバッファ + CSV 書き出し} \\
\>\>\> fatfs/ \quad\textrm{\color{gray}← ChaN's FatFs R0.11a + diskio ブリッジ} \\
\>\>\> video/ \quad\textrm{\color{gray}← VDC5/DVDEC ドライバ群 (DisplayBase API)} \\
\>\> system/ \\
\>\>\> system\_init.c \quad\textrm{\color{gray}← FPU/MMU/L1 キャッシュ有効化} \\
\>\>\> mmu\_rzA1H.c \quad\textrm{\color{gray}← MMU 翻訳テーブル生成} \\
\> generate/ \quad\textrm{\color{gray}← e2 studio 自動生成（一部手動修正あり）} \\
\>\> iodefine.h \quad\textrm{\color{gray}← レジスタ定義} \\
\>\> inthandler.c \quad\textrm{\color{gray}← 割り込みディスパッチ ★手動修正} \\
\>\> start.S / linker\_script.ld \quad\textrm{\color{gray}← 起動コード・リンカスクリプト ★手動修正} \\
\> tests/ \quad\textrm{\color{gray}← PC 上の単体テスト (CMake + レジスタモック)} \\
\> doc/ \\
\>\> main.tex / main.pdf \quad\textrm{\color{gray}← 本仕様書} \\
\> .devcontainer/ \quad\textrm{\color{gray}← LaTeX ビルド用 DevContainer} \\
\end{tabbing}
\end{tcolorbox}

\subsection{ディレクトリ役割}

\begin{tabularx}{\textwidth}{l X}
\toprule
\textbf{ディレクトリ} & \textbf{役割} \\
\midrule
\texttt{src/core/}    & システム基盤（\texttt{IModule}, \texttt{SystemData}, \texttt{ProjectConfig}, \texttt{ModuleTimer}） \\
\texttt{src/logic/}   & Logic フェーズの走行ロジック（\texttt{RunLogic}）。ハードウェアに触れない \\
\texttt{src/drivers/} & ハードウェアドライバ（Camera, LineDetector, Motor, Servo, Encoder, Onboard, Serial, SDCard, SDLogger） \\
\texttt{src/drivers/fatfs/} & ChaN's FatFs R0.11a と \texttt{SDCard} へのブリッジ（\texttt{diskio.cpp}） \\
\texttt{src/drivers/video/} & \texttt{mbed-gr-libs} 由来の VDC5/DVDEC ドライバ群と \texttt{DisplayBase} API \\
\texttt{src/system/}  & FPU / MMU / L1 キャッシュ初期化（mbed OS の \texttt{SystemInit()} 移植） \\
\texttt{generate/}    & e2 studio 自動生成ファイル（\hyperref[sec:generate]{§\ref*{sec:generate}} の手動修正あり） \\
\texttt{tests/}       & PC 上で動かす単体テスト（レジスタをモック化して Motor/Servo/Encoder を検証） \\
\texttt{doc/}         & 本仕様書（\texttt{main.tex} / \texttt{main.pdf}） \\
\texttt{.devcontainer/} & LaTeX ドキュメントビルド用のコンテナ環境定義 \\
\bottomrule
\end{tabularx}

\subsection{全ファイル一覧}

\begin{longtable}{l p{92mm}}
\toprule
\textbf{ファイル} & \textbf{役割} \\
\midrule
\endhead
\texttt{src/mcr\_camera\_2026base.cpp} & メインエントリ・GIC/OSTM0 初期化・3フェーズ割り込みハンドラ・2つの動作モードループ \\
\texttt{src/core/IModule.h}         & 全モジュール共通の基底インターフェース \\
\texttt{src/core/SystemData.h}      & 全モジュールの Data を集約した共有構造体 \\
\texttt{src/core/ProjectConfig.h}   & 全 \texttt{*\_CONFIG} インスタンスの実値定義（調整の入口） \\
\texttt{src/core/ModuleTimer.h}     & \texttt{g\_timer\_1ms} を時刻源とするノンブロッキングタイマー \\
\texttt{src/logic/RunLogic.h/.cpp}  & 走行状態機械（\texttt{applyDrivingPattern()}）・\texttt{RunPattern} 定義 \\
\texttt{src/drivers/Camera.h/.cpp}  & NTSC 160$\times$120 キャプチャ（VDC5 ch0 + DVDEC0） \\
\texttt{src/drivers/LineDetector.h/.cpp} & 偏差算出・クロス/ハーフ/センターライン検出・8点センサ生成 \\
\texttt{src/drivers/Motor.h/.cpp}   & モータ PWM 制御 (MTU2 ch3/4 リセット同期PWM) \\
\texttt{src/drivers/Servo.h/.cpp}   & サーボ PWM 制御 (MTU2 ch0 PWMモード1) \\
\texttt{src/drivers/Encoder.h/.cpp} & ロータリーエンコーダ読取 (MTU2 ch1 位相計数モード2) \\
\texttt{src/drivers/Onboard.h/.cpp} & オンボード LED・スイッチ制御 (GPIO ポート6) \\
\texttt{src/drivers/Serial.h/.cpp}  & デバッグシリアル通信 (SCIF2, 230400bps) \\
\texttt{src/drivers/SDCard.h/.cpp}  & SD カード SPI ブロックドライバ (RSPI2)。FatFs の下位層 \\
\texttt{src/drivers/SDLogger.h/.cpp}& 走行ログのバッファリングと CSV 書き出し \\
\texttt{src/drivers/fatfs/ff.c}     & FatFs R0.11a 本体 \\
\texttt{src/drivers/fatfs/diskio.cpp} & FatFs $\rightarrow$ \texttt{SDCard} のディスク I/O ブリッジ \\
\texttt{src/drivers/video/}         & VDC5/DVDEC ドライバ群と \texttt{DisplayBase} API (40 ファイル超) \\
\texttt{src/system/system\_init.c}  & FPU / MMU / L1 キャッシュ有効化 \\
\texttt{generate/inthandler.c}      & IRQ ディスパッチャと OSTM0 コールバック呼び出し（手動修正） \\
\texttt{generate/linker\_script.ld} & \texttt{.init\_array} 収集を追加したリンカスクリプト（手動修正） \\
\texttt{generate/start.S}           & 起動コード（VBAR 設定・IRQ スタック確保を追加、手動修正） \\
\bottomrule
\end{longtable}

\subsection{機能とファイルの対応表}

\begin{tabularx}{\textwidth}{l l X}
\toprule
\textbf{機能} & \textbf{メインファイル} & \textbf{関連ファイル} \\
\midrule
3フェーズ割り込み & \texttt{mcr\_camera\_2026base.cpp} & IModule.h, SystemData.h, inthandler.c \\
走行状態機械     & \texttt{RunLogic.cpp}    & SystemData.h, ProjectConfig.h, ModuleTimer.h \\
ライン検出       & \texttt{LineDetector.cpp}& Camera.h, SystemData.h, ProjectConfig.h \\
カメラ入力       & \texttt{Camera.cpp}      & video/DisplayBace.h, video/gr\_peach\_vdc5.c \\
モータ制御       & \texttt{Motor.cpp}       & IModule.h, iodefine.h \\
サーボ制御       & \texttt{Servo.cpp}       & IModule.h, iodefine.h \\
エンコーダ       & \texttt{Encoder.cpp}     & IModule.h, iodefine.h \\
LED・スイッチ    & \texttt{Onboard.cpp}     & IModule.h, iodefine.h \\
シリアル通信     & \texttt{Serial.cpp}      & IModule.h, iodefine.h \\
走行ログ         & \texttt{SDLogger.cpp}    & SDCard.cpp, fatfs/ff.c, fatfs/diskio.cpp \\
起動時初期化     & \texttt{system\_init.c}  & mmu\_rzA1H.c, start.S \\
\bottomrule
\end{tabularx}

%% ============================================================
%% 6. 技術スタック
%% ============================================================
\section{技術スタック}\label{sec:techstack}

\begin{tabularx}{\textwidth}{l X}
\toprule
\textbf{項目} & \textbf{内容} \\
\midrule
ターゲットボード & GR-PEACH (ルネサス RZ/A1H, ARM Cortex-A9) \\
開発環境         & Renesas e2 studio（ビルド構成 \texttt{HardwareDebug}） \\
言語             & C++（ベアメタル、mbed OS 不使用）。\texttt{ProjectConfig.h} が
                   \texttt{inline const} を使うため \textbf{C++17 以上}が必要 \\
コンパイラ       & GCC for Renesas ARM (\texttt{arm-none-eabi-gcc/g++} 13.3.1) \\
浮動小数点       & VFPv3-D16, hard ABI, リトルエンディアン \\
実行方式         & XIP（SPI フラッシュ直接実行）+ MMU / L1 キャッシュ有効
                   (\hyperref[sec:xip]{§\ref*{sec:xip}}) \\
レジスタ定義     & \texttt{iodefine.h}（e2 studio 自動生成） \\
割り込み         & GIC (Generic Interrupt Controller) + OSTM0 による 1ms インターバル \\
映像ドライバ     & \texttt{mbed-gr-libs} 由来の VDC5/DVDEC ドライバ + \texttt{DisplayBase} API \\
ファイルシステム & ChaN's FatFs R0.11a（\texttt{diskio.cpp} 経由で \texttt{SDCard} に接続） \\
ドキュメント     & \LaTeX (uplatex + dvipdfmx, \texttt{latexmk})。DevContainer で環境統一 \\
\bottomrule
\end{tabularx}

%% ============================================================
%% 7. API仕様 (データシート)
%% ============================================================
\section{API仕様}\label{sec:api}

本章では各モジュールの公開APIをデータシート形式で記述する。
\texttt{src/drivers/} 配下の全クラスは \texttt{IModule} インターフェース
(\hyperref[sec:api-imodule]{§\ref*{sec:api-imodule}}) を実装する。
Logic レイヤーの \texttt{RunLogic}
(\hyperref[sec:api-runlogic]{§\ref*{sec:api-runlogic}}) だけはクラスではなく
自由関数の集合であり、\texttt{IModule} を継承しない。

\begin{notebox}[定数値の扱い]
各クラスの \texttt{static const}（\texttt{Motor::MAX\_POWER} 等）は後方互換のために
残されているもので、\textbf{実行時に使われるのは \texttt{\_config} 経由の値}である。
実値の一覧は \hyperref[sec:params]{§\ref*{sec:params}} を参照すること。
\end{notebox}

%% ── 7.1 IModule ──
\subsection{IModule インターフェース}\label{sec:api-imodule}

\begin{apibox}{IModule --- 基底インターフェース}

\textbf{ファイル:} \texttt{src/core/IModule.h} \hfill
\textbf{種別:} 純粋仮想クラス

\vspace{2mm}\hrule\vspace{2mm}

\apisection{概要}

全てのドライバが実装する共通インターフェース。
3フェーズ実行モデル (\hyperref[sec:threephase]{§\ref*{sec:threephase}}) に対応し、
入力と出力を別メソッドに分けている点が特徴である。

\begin{itemize}[nosep]
  \item 入力専用モジュール（Camera, Encoder）は \texttt{updateInput()} のみを override する
  \item 出力専用モジュール（Motor, Servo, Serial, SDCard, SDLogger）は \texttt{updateOutput()} のみを override する
  \item 入出力両方のモジュール（Onboard）は両方を override する
\end{itemize}

\texttt{SystemData} は前方宣言のみを行い、各モジュールヘッダから
\texttt{SystemData.h} を include させないことで循環依存を避けている。

\apisection{関数ドキュメント}

\begin{funcblock}{init()}
\texttt{virtual bool init() = 0}

\vspace{1.5mm}
初期化処理。レジスタ設定・変数初期化を行う。システム起動時に \texttt{main()} から1回のみ呼び出される。

\vspace{1.5mm}
\textbf{戻り値:} \texttt{bool} --- \texttt{true}=成功 / \texttt{false}=失敗。
失敗時は呼び出し側で \texttt{enabled = false} に落とす運用を推奨する。
\end{funcblock}

\begin{funcblock}{updateInput(sys)}
\texttt{virtual void updateInput(SystemData\& sys)}

\vspace{1.5mm}
入力フェーズ。ハードウェアを読み取り、結果を \texttt{sys} の自モジュール領域へ書き込む。
デフォルトは空実装のため、出力専用モジュールは override 不要。
\end{funcblock}

\begin{funcblock}{updateOutput(sys)}
\texttt{virtual void updateOutput(SystemData\& sys)}

\vspace{1.5mm}
出力フェーズ。\texttt{sys} の指示値（\texttt{*Cmd}）を読み、ハードウェアへ反映する。
デフォルトは空実装のため、入力専用モジュールは override 不要。
\end{funcblock}

\begin{funcblock}{deinit()}
\texttt{virtual void deinit()}

\vspace{1.5mm}
終了処理（バス停止、リソース解放等）。デフォルトは何もしない。
\end{funcblock}

\apisection{メンバ変数}

\begin{tabularx}{\textwidth}{l l l X}
\toprule
\textbf{変数名} & \textbf{型} & \textbf{初期値} & \textbf{説明} \\
\midrule
\texttt{enabled} & \texttt{bool} & \texttt{true} &
  \texttt{false} の間は呼び出し側の配列ループが
  \texttt{updateInput}/\texttt{updateOutput} をスキップする \\
\bottomrule
\end{tabularx}

\apisection{インターフェース定義}

\begin{lstlisting}[caption={IModule インターフェース定義},label=lst:imodule]
struct SystemData;   // 前方宣言（循環依存の回避）

class IModule {
public:
  virtual ~IModule() {}

  // 初期化処理（成功 = true、失敗 = false）
  virtual bool init() = 0;

  // 入力フェーズ: ハードウェア -> SystemData
  virtual void updateInput(SystemData& sys) { (void)sys; }

  // 出力フェーズ: SystemData -> ハードウェア
  virtual void updateOutput(SystemData& sys) { (void)sys; }

  // 終了処理（バス停止、リソース解放等）
  virtual void deinit() {}

  // false の間は updateInput / updateOutput をスキップする
  bool enabled = true;
};
\end{lstlisting}

\apisection{新規モジュールの追加手順}

\begin{enumerate}[nosep, leftmargin=*]
  \item \texttt{src/drivers/Xxx.h} に \texttt{XxxConfig} と \texttt{XxxData} を宣言する
  \item \texttt{class Xxx : public IModule} を定義し、\texttt{explicit Xxx(const XxxConfig\&)} を用意する
  \item \texttt{SystemData.h} に \texttt{XxxData xxx;} を追加し、ヘッダを include する
  \item \texttt{ProjectConfig.h} に \texttt{XXX\_CONFIG} の実値を定義する
  \item \texttt{mcr\_camera\_2026base.cpp} でグローバルインスタンス \texttt{g\_xxx} を生成し、
        \texttt{main()} で \texttt{init()} を呼び、必要なフェーズの配列へ登録する
\end{enumerate}

\end{apibox}

%% ── 7.2 Encoder ──
\subsection{Encoder クラス}\label{sec:api-encoder}

\begin{apibox}{Encoder --- エンコーダドライバ}

\textbf{ファイル:} \texttt{src/drivers/Encoder.h}, \texttt{Encoder.cpp} \hfill
\textbf{継承:} \texttt{IModule} \hfill
\phasebadge{Input フェーズ}

\vspace{2mm}\hrule\vspace{2mm}

\apisection{概要}

MTU2 チャネル1の位相計数モード2を使用し、エンコーダのパルスをカウントする。
1ms ごとに \texttt{MTU2TCNT\_1} を読んでゼロクリアし、
その差分（1ms あたりのパルス数）を累積カウンタと2種類のフィルタに通す。

\begin{cautionbox}[現状 ISR に未接続]
\texttt{g\_encoder} のインスタンス自体は生成されているが、
\texttt{main()} での \texttt{init()} 呼び出しと \texttt{s\_inputModules[]} への登録は
まだ行われていない。したがって \texttt{sys.enc.*} は常に 0 のままである。
SDLogger 連携が必要になった時点で有効化する予定。
\end{cautionbox}

\apisection{機能一覧}

\begin{itemize}[nosep]
  \item MTU2 位相計数モード2による前進/後退を区別したパルス計数
  \item 走行距離換算用の累積カウント（\texttt{totalCount}）とクランク検出用（\texttt{magaCount}）の2系統
  \item 移動平均フィルタ（段数 \texttt{filterN}）
  \item RC フィルタ（指数移動平均、係数 \texttt{rcAlpha}）
\end{itemize}

\apisection{設定 (EncoderConfig)}

\begin{tabularx}{\textwidth}{l l l X}
\toprule
\textbf{メンバ} & \textbf{型} & \textbf{実値} & \textbf{説明} \\
\midrule
\texttt{filterN}  & \texttt{int}   & 4    & 移動平均の段数（上限は \texttt{Encoder::FILTER\_N = 4}） \\
\texttt{rcAlpha}  & \texttt{float} & 0.5  & RC フィルタ係数 $\alpha$ \\
\texttt{aPinFmt}  & \texttt{int}   & 0    & A相 P1\_0 (TCLKA) \\
\texttt{bPinFmt}  & \texttt{int}   & 10   & B相 P1\_10 (TCLKB) \\
\bottomrule
\end{tabularx}

\apisection{出力データ (EncoderData / sys.enc)}

\begin{tabularx}{\textwidth}{l l X}
\toprule
\textbf{メンバ} & \textbf{型} & \textbf{説明} \\
\midrule
\texttt{totalCount}  & \texttt{int}   & 走行距離換算用の累積カウント \\
\texttt{magaCount}   & \texttt{int}   & クランク検出用の累積カウント \\
\texttt{cnt}         & \texttt{int}   & 直近 1ms あたりのカウント（符号付き） \\
\texttt{avgCnt}      & \texttt{int}   & 移動平均後のカウント \\
\texttt{filteredCnt} & \texttt{float} & RC フィルタ後のカウント \\
\bottomrule
\end{tabularx}

\begin{notebox}[距離ではなくパルス数を扱う]
本実装が保持しているのは\textbf{パルスカウント（\texttt{int}）}であり、
距離 [mm] や速度への換算は行っていない。
走行状態の遷移条件は経過時間 [ms] で管理する方針
(\hyperref[sec:api-runlogic]{§\ref*{sec:api-runlogic}}) のため、
距離換算係数（1パルスあたりの移動距離）は定義していない。
\end{notebox}

\apisection{関数ドキュメント}

\begin{funcblock}{init()}
\texttt{bool init() override}

\vspace{1.5mm}
MTU2 チャネル1を位相計数モード2で初期化する。
P1\_0 / P1\_10 のピン設定、MTU2 のスタンバイ解除、カウンタスタートを行う。
\end{funcblock}

\begin{funcblock}{updateInput(sys)}
\texttt{void updateInput(SystemData\& sys) override}

\vspace{1.5mm}
\texttt{MTU2TCNT\_1} を符号付き 16bit として読み出して 0 にリセットし、
累積カウンタ・移動平均・RC フィルタを更新して \texttt{sys.enc.*} に格納する。
RC フィルタは $\mathit{rc} \leftarrow \alpha \cdot \mathit{rc} + (1-\alpha) \cdot \mathit{cnt}$ で更新する。
\end{funcblock}

\begin{funcblock}{clearTotal() / clearMaga()}
\texttt{void clearTotal()} / \texttt{void clearMaga()}

\vspace{1.5mm}
それぞれ \texttt{totalCount\_} / \texttt{magaCount\_} を 0 にリセットする。
\end{funcblock}

\begin{funcblock}{後方互換 getter}
\texttt{int getTotalCount() const} / \texttt{int getMagaCount() const} /
\texttt{int getCnt() const} / \texttt{float getFilteredCnt() const}

\vspace{1.5mm}
内部状態を直接返す。\texttt{getCnt()} は移動平均後の値（\texttt{avg\_}）を返す点に注意。
新規コードでは \texttt{sys.enc.*} を読むこと。
\end{funcblock}

\apisection{ハードウェアマッピング}

\begin{tabularx}{\textwidth}{l l l X}
\toprule
\textbf{信号} & \textbf{ピン} & \textbf{レジスタ} & \textbf{説明} \\
\midrule
A相 & P1\_0  (TCLKA) & MTU2TCNT\_1 & 位相計数モード2の入力 \\
B相 & P1\_10 (TCLKB) & MTU2TCNT\_1 & 同上（位相差で方向を判別） \\
\bottomrule
\end{tabularx}

\end{apibox}

%% ── 7.3 Camera ──
\subsection{Camera クラス}\label{sec:api-camera}

\begin{apibox}{Camera --- NTSC ビデオキャプチャドライバ}

\textbf{ファイル:} \texttt{src/drivers/Camera.h}, \texttt{Camera.cpp} \hfill
\textbf{継承:} \texttt{IModule} \hfill
\phasebadge{Input フェーズ}

\vspace{2mm}\hrule\vspace{2mm}

\apisection{概要}

VDC5 チャネル0 と DVDEC0 を用いて NTSC 映像を 160$\times$120 の YCbCr422 として取り込み、
輝度（Y）成分のみを抽出して 160$\times$120 のグレースケールバッファに格納する。
初期化には \texttt{mbed-gr-libs} 由来の \texttt{DisplayBase} API
（\texttt{Graphics\_init} $\rightarrow$ \texttt{Graphics\_Video\_init} $\rightarrow$
\texttt{Video\_Write\_Setting}）を使用する。

\apisection{機能一覧}

\begin{itemize}[nosep]
  \item VDC5 + DVDEC による NTSC インターレース映像のキャプチャ
  \item フレーム処理の4ステップ分割（XIP 環境の 1ms 制約に対応）
  \item Vfield 割り込みによるフィールド同期
  \item 座標指定の輝度取得 (\texttt{getPixel}) と 8点センサ変換 (\texttt{thresholdConvert})
\end{itemize}

\apisection{設定 (CameraConfig)}

\begin{tabularx}{\textwidth}{l l X}
\toprule
\textbf{メンバ} & \textbf{実値} & \textbf{説明} \\
\midrule
\texttt{pixelWidth}       & 160 & 水平ピクセル数 \\
\texttt{pixelHeight}      & 120 & 垂直ピクセル数 \\
\texttt{thresholdDefault} & 170 & 8点センサ取得時のデフォルト閾値 \\
\texttt{thresholdRow}     & 60  & 8点センサ取得行 \\
\texttt{thresholdDiff}    & 8   & 8点センサ閾値の差分 \\
\bottomrule
\end{tabularx}

\apisection{出力データ (CameraData / sys.cam)}

\begin{tabularx}{\textwidth}{l l X}
\toprule
\textbf{メンバ} & \textbf{型} & \textbf{説明} \\
\midrule
\texttt{frameReady}     & \texttt{bool}     & 新フレーム完了フラグ。メインループが消費後に \texttt{false} へ戻す \\
\texttt{frameCount}     & \texttt{uint32\_t}& 累積フレーム数（デバッグ表示用） \\
\texttt{field}          & \texttt{int}      & 0=トップ / 1=ボトムフィールド \\
\texttt{imageBufferPtr} & \texttt{const volatile unsigned char*} & 輝度バッファ（160$\times$120）へのポインタ \\
\bottomrule
\end{tabularx}

\apisection{フレーム処理の4ステップ分割}

XIP 実行環境では \texttt{imageCopy()} 1回に約 7--8ms を要し、1ms 割り込みに収まらない
(\hyperref[sec:xip]{§\ref*{sec:xip}})。そのため1フレームの処理を4ステップに分割し、
1ms 割り込みごとに1ステップだけ進める。

\begin{tabularx}{\textwidth}{c l X}
\toprule
\textbf{ステップ} & \textbf{処理} & \textbf{内容} \\
\midrule
0 & \texttt{imageCopy(0)}         & Vfield 値を \texttt{capturedField\_} にキャプチャし、前半（0--59行）をコピー \\
1 & \texttt{imageCopy(1)}         & 後半（60--119行）をコピー \\
2 & \texttt{extractBrightness(0)} & 前半の Y 成分を抽出 \\
3 & \texttt{extractBrightness(1)} & 後半の Y 成分を抽出。完了時に \texttt{frameReady\_} を立てる \\
4以降 & Vfield 待ち              & 次の Vfield 発生まで即リターンし、メインループへ CPU 時間を返す \\
\bottomrule
\end{tabularx}

\begin{notebox}[capturedField\_ 方式が必要な理由]
XIP 環境では4ステップの合計が NTSC の Vfield 間隔（16.7ms）を超えるため、
\texttt{imageCopy()} と \texttt{extractBrightness()} がそれぞれ Vfield トグルを読み直すと
\textbf{異なるフィールド値を使ってしまい画像が乱れる}。
ステップ0で1回だけ値を確保し、4ステップ全体で同じ値を使うことでこれを防いでいる。
また、フレーム処理中（ステップ0--3）は Vfield 変化を無視する。
処理中にリセットを掛けるとステップ1以降へ進めず、輝度バッファが永久に更新されない。
\end{notebox}

\apisection{関数ドキュメント}

\begin{funcblock}{init()}
\texttt{bool init() override}

\vspace{1.5mm}
\texttt{DisplayBase} API で VDC5 / DVDEC を初期化し（NTSC 160$\times$120 YCbCr422 入力）、
メモリ書き込み設定を行ってビデオキャプチャを開始する。
初期化中の Vsync / Vfield 待ちは割り込みベースで行うため、
\textbf{事前に GIC が有効化されている必要がある} (\hyperref[sec:bootseq]{§\ref*{sec:bootseq}})。
\end{funcblock}

\begin{funcblock}{updateInput(sys)}
\texttt{void updateInput(SystemData\& sys) override}

\vspace{1.5mm}
1ms ごとに呼ばれ、上表のステップを1つ進める。
毎周期の末尾で \texttt{sys.cam.*} を最新状態に更新する。
メインループが \texttt{sys.cam.frameReady} を \texttt{false} に戻すと内部状態もリセットされる。
\end{funcblock}

\begin{funcblock}{getPixel(x, y)}
\texttt{unsigned char getPixel(int x, int y) const}

\vspace{1.5mm}
輝度バッファから座標 $(x, y)$ のピクセル値を返す。

\vspace{1.5mm}
\textbf{引数:} \texttt{x} (0--159), \texttt{y} (0--119)\quad
\textbf{戻り値:} \texttt{unsigned char} --- 輝度 (0--255)
\end{funcblock}

\begin{funcblock}{getImageBuffer()}
\texttt{const volatile unsigned char* getImageBuffer() const}

\vspace{1.5mm}
輝度バッファ先頭への読み取り専用ポインタを返す。
\end{funcblock}

\begin{funcblock}{thresholdConvert(gyou, threshold, diff)}
\texttt{unsigned char thresholdConvert(int gyou, int threshold, int diff) const}

\vspace{1.5mm}
参考プロジェクトの \texttt{shikiichi\_henkan} 相当。
指定行の8点（$x = 31, 43, 54, 71, 88, 105, 116, 128$）の輝度を取得し、
閾値を自動調整して2進数8ビットに変換する。

\vspace{1.5mm}
\textbf{引数:} \texttt{gyou} --- 行番号 (0--119), \texttt{threshold} --- 閾値, \texttt{diff} --- 差分閾値

\textbf{戻り値:} \texttt{unsigned char} --- 8点センサ値
\end{funcblock}

\begin{funcblock}{vfieldCallback / vsyncCallback \normalfont\small\color{gray}[static]}
\texttt{static void vfieldCallback(DisplayBase::int\_type\_t)} /
\texttt{static void vsyncCallback(DisplayBase::int\_type\_t)}

\vspace{1.5mm}
VDC5 の Vfield / Vsync 割り込みから呼ばれ、内部のフィールドトグルと
カウンタを更新する。フレーム同期の起点となる。
\end{funcblock}

\apisection{内部変数（抜粋）}

\begin{tabularx}{\textwidth}{l l X}
\toprule
\textbf{変数名} & \textbf{型} & \textbf{説明} \\
\midrule
\texttt{frameStep\_}     & \texttt{int}  & フレーム処理ステップカウンタ (0--4) \\
\texttt{fieldToggle\_}   & \texttt{volatile int} & トップ/ボトムフィールドのトグル \\
\texttt{capturedField\_} & \texttt{int}  & ステップ0で確保したフィールド値 \\
\texttt{imageBuffer\_}   & \texttt{volatile unsigned char[19200]} & 輝度データ（最終出力） \\
\texttt{ycbcrBuffer\_}   & \texttt{unsigned char[38400]} & YCbCr422 中間バッファ \\
\bottomrule
\end{tabularx}

\begin{cautionbox}[廃止された API]
旧仕様の \texttt{isFrameReady()} / \texttt{clearFrameReady()} は\textbf{廃止済み}である。
フレーム完了の検知は \texttt{sys.cam.frameReady} を直接読み書きする方式に変更された。
また、旧仕様に記載されていた画像処理 API
（\texttt{resize}, \texttt{binarize}, \texttt{percentileMethod},
\texttt{discriminantAnalysis}, \texttt{extractPart},
\texttt{standardDeviation}, \texttt{covariance}, \texttt{matchPattern}）は
\textbf{いずれも実装されていない}。ライン検出は \texttt{LineDetector}
(\hyperref[sec:api-linedetector]{§\ref*{sec:api-linedetector}}) が担当する。
\end{cautionbox}

\end{apibox}

%% ── 7.4 LineDetector ──
\subsection{LineDetector クラス}\label{sec:api-linedetector}

\begin{apibox}{LineDetector --- ライン検出モジュール}

\textbf{ファイル:} \texttt{src/drivers/LineDetector.h}, \texttt{LineDetector.cpp} \hfill
\textbf{継承:} \texttt{IModule} \hfill
\phasebadge{Input フェーズ（メインループ実行）}

\vspace{2mm}\hrule\vspace{2mm}

\apisection{概要}

\texttt{Camera} が生成した輝度バッファからコースの白線を検出し、
各行の偏差・各種ラインフラグ・8点センサ値を \texttt{sys.line.*} に格納する。
PID 制御は行わず、ハンドル角の算出は \texttt{RunLogic} 側の
\texttt{calcHandle()}（比例ゲイン3段切替）が担当する。

\begin{cautionbox}[ISR ではなくメインループから呼ぶ]
処理が HEIGHT $\times$ WIDTH のループで重いため、1ms 割り込み内では呼ばない。
メインループが \texttt{sys.cam.frameReady == true} を検出したときにのみ
\texttt{g\_lineDetector.updateInput(g\_sys)} を実行する。
このため \texttt{s\_inputModules[]} 配列にも登録されていない。
\end{cautionbox}

\apisection{機能一覧}

\begin{itemize}[nosep]
  \item 全120行分の偏差算出（\texttt{deviation[]}）
  \item クロスライン / 左ハーフライン / 右ハーフライン / センターラインの検出
  \item 8点センサ値（\texttt{sensorBin}）の生成
  \item 走行パターン（\texttt{sys.run.pattern}）に応じた検出パラメータの自動切替
\end{itemize}

\apisection{クラス定数}

\begin{tabularx}{\textwidth}{l l X}
\toprule
\textbf{定数名} & \textbf{値} & \textbf{説明} \\
\midrule
\texttt{WIDTH}  & 160 & 画像幅 \\
\texttt{HEIGHT} & 120 & 画像高さ（\texttt{deviation[]} の要素数と一致させること） \\
\texttt{CENTER} & 80  & 画像中心の $x$ 座標。偏差 0 の基準 \\
\bottomrule
\end{tabularx}

\apisection{出力データ (LineDetectorData / sys.line)}

\begin{tabularx}{\textwidth}{l l X}
\toprule
\textbf{メンバ} & \textbf{型} & \textbf{説明} \\
\midrule
\texttt{deviation[120]} & \texttt{int}  & 各行の偏差。正=ラインが左寄り、負=ラインが右寄り \\
\texttt{crossLine}   & \texttt{bool} & クロスライン検出フラグ \\
\texttt{leftLine}    & \texttt{bool} & 左ハーフライン検出フラグ \\
\texttt{rightLine}   & \texttt{bool} & 右ハーフライン検出フラグ \\
\texttt{centerLine}  & \texttt{bool} & センターライン検出フラグ \\
\texttt{sensorBin}   & \texttt{unsigned char} & 8点センサ値（ビット列） \\
\texttt{detectRow}   & \texttt{int}  & 直近で使用された検出行（デバッグ表示用） \\
\bottomrule
\end{tabularx}

\apisection{パターン別パラメータの切替}

\texttt{sys.run.pattern} の値に応じて、3組のパラメータセット
（\texttt{LineDetectorPatternParams}）を切り替える。

\begin{tabularx}{\textwidth}{l l X}
\toprule
\textbf{パラメータセット} & \textbf{適用条件} & \textbf{用途} \\
\midrule
\texttt{patternNormal} & \texttt{pattern == 11} (\texttt{RP\_TRACE\_NORMAL}) & 通常トレース時 \\
\texttt{patternStart}  & \texttt{pattern == 1} または \texttt{2} & スタートバー接近・待機時 \\
\texttt{patternOther}  & 上記以外 & クランク・レーンチェンジ等 \\
\bottomrule
\end{tabularx}

\vspace{2mm}
各セットのメンバの意味は次のとおり。負値には特別な意味がある。

\begin{tabularx}{\textwidth}{l X}
\toprule
\textbf{メンバ} & \textbf{意味} \\
\midrule
\texttt{crosslineWidth}      & クロスライン判定に使う幅。$<0$ なら実行時に \texttt{detectRow\_ + 20} を算出 \\
\texttt{detectHeight}        & 検出に使う行の厚み \\
\texttt{crossCountThreshold} & クロスライン判定の画素数閾値。$<0$ なら \texttt{crosslineWidth} との加算値 \\
\texttt{brightnessRatio}     & 最大輝度に対する相対閾値の比率 \\
\texttt{brightnessAbs}       & $>0$ なら絶対閾値として使用、$=0$ なら \texttt{brightnessRatio} を使用 \\
\texttt{centerWidth}         & センターライン判定に使う幅 \\
\bottomrule
\end{tabularx}

\vspace{2mm}
実値は \hyperref[sec:params-line]{§\ref*{sec:params-line}} を参照。

\apisection{関数ドキュメント}

\begin{funcblock}{init()}
\texttt{bool init() override}

\vspace{1.5mm}
偏差配列とフラグ類をゼロクリアし、検出行・検出高さを Config の既定値へ戻す。
グローバルコンストラクタが走らない環境への保険として、
コンストラクタと同じ初期化をここでも明示的に行っている。
\end{funcblock}

\begin{funcblock}{updateInput(sys)}
\texttt{void updateInput(SystemData\& sys) override}

\vspace{1.5mm}
以下を順に実行し、結果を \texttt{sys.line.*} へ書き込む。

\begin{enumerate}[nosep, leftmargin=*]
  \item \texttt{sys.run.pattern} を \texttt{pattern\_} へコピー（パラメータ切替に使用）
  \item \texttt{calcDeviation()} --- 全行の偏差を算出
  \item \texttt{calcLineFlags()} --- クロス/左右ハーフ/センターの各フラグを判定
  \item \texttt{updateSensorBin()} --- 8点センサ値を生成
\end{enumerate}
\end{funcblock}

\begin{funcblock}{getDeviation(row)}
\texttt{int getDeviation(int row) const}

\vspace{1.5mm}
指定行の偏差を返す。範囲外（$row < 0$ または $row \geq 120$）の場合は 0 を返す。
\end{funcblock}

\begin{funcblock}{フラグ getter}
\texttt{bool isCrossLine() const} / \texttt{isLeftLine()} / \texttt{isRightLine()} / \texttt{isCenterLine()}

\vspace{1.5mm}
各ラインフラグを返す。デバッグ表示用の後方互換 API であり、
走行ロジックからは \texttt{sys.line.*} を読むこと。
\end{funcblock}

\begin{funcblock}{getSensorBin() / sensorInput(mask)}
\texttt{unsigned char getSensorBin() const} /
\texttt{unsigned char sensorInput(unsigned char mask) const}

\vspace{1.5mm}
8点センサ値をそのまま、またはマスクを適用して返す。
マスク定数は \texttt{MASK2\_2}(0x66), \texttt{MASK4\_4}(0xff) 等がヘッダに定義されている。
\end{funcblock}

\end{apibox}

%% ── 7.5 Motor ──
\subsection{Motor クラス}\label{sec:api-motor}

\begin{apibox}{Motor --- モータ PWM ドライバ}

\textbf{ファイル:} \texttt{src/drivers/Motor.h}, \texttt{Motor.cpp} \hfill
\textbf{継承:} \texttt{IModule} \hfill
\phasebadge{Output フェーズ}

\vspace{2mm}\hrule\vspace{2mm}

\apisection{概要}

MTU2 チャネル3/4 を使用したリセット同期 PWM モードで左右モータを制御する。
出力指示は \texttt{sys.mot.leftCmd} / \texttt{rightCmd} に $-100 \sim +100$ [\%] の
\textbf{整数}で与え、\texttt{updateOutput()} が PWM デューティと方向ピンへ反映する。

\apisection{機能一覧}

\begin{itemize}[nosep]
  \item MTU2 リセット同期 PWM モードによる左右独立駆動
  \item 符号による正転/逆転の方向制御
  \item \texttt{maxPower} による出力上限クランプ
  \item 反映後の値を \texttt{sys.mot.*Actual} へ書き戻し（ログ・デバッグ用）
\end{itemize}

\apisection{設定 (MotorConfig)}

\begin{tabularx}{\textwidth}{l l X}
\toprule
\textbf{メンバ} & \textbf{実値} & \textbf{説明} \\
\midrule
\texttt{pwmCycle}       & 33332 & PWM 周期カウント値 (1ms @ P0$\phi$/1) \\
\texttt{maxPower}       & 70    & 出力上限 [\%]。\textbf{実効的な最大出力はこの値} \\
\texttt{leftPwmPinFmt}  & 4     & P4\_4 (TIOC4A) \\
\texttt{rightPwmPinFmt} & 5     & P4\_5 (TIOC4B) \\
\texttt{leftDirPinFmt}  & 6     & P4\_6 (GPIO) \\
\texttt{rightDirPinFmt} & 7     & P4\_7 (GPIO) \\
\bottomrule
\end{tabularx}

\begin{notebox}[MAX\_POWER と maxPower は別物]
ヘッダの \texttt{Motor::MAX\_POWER = 100} は後方互換のために残る \texttt{static const} であり、
実行時のクランプに使われるのは \texttt{MOTOR\_CONFIG.maxPower = 70} のほうである。
また \texttt{RunLogic::calcMotorVal()} は通常走行時のモータ出力を
\texttt{MOTOR\_CONFIG.maxPower} そのものに設定するため、
\textbf{この1項目を変えると通常走行速度が直接変わる}。
\end{notebox}

\apisection{入出力データ (MotorData / sys.mot)}

\begin{tabularx}{\textwidth}{l l l X}
\toprule
\textbf{メンバ} & \textbf{型} & \textbf{方向} & \textbf{説明} \\
\midrule
\texttt{leftCmd}     & \texttt{int} & Logic $\rightarrow$ Motor & 左モータ指示値 $[-100, +100]$ \\
\texttt{rightCmd}    & \texttt{int} & Logic $\rightarrow$ Motor & 右モータ指示値 $[-100, +100]$ \\
\texttt{leftActual}  & \texttt{int} & Motor $\rightarrow$ 外部  & クランプ後に実際に反映した値 \\
\texttt{rightActual} & \texttt{int} & Motor $\rightarrow$ 外部  & 同上 \\
\bottomrule
\end{tabularx}

\apisection{関数ドキュメント}

\begin{funcblock}{init()}
\texttt{bool init() override}

\vspace{1.5mm}
P4\_4/P4\_5 をペリフェラル（PWM 出力）に、P4\_6/P4\_7 を GPIO 出力（方向制御）に設定し、
MTU2 のスタンバイを解除してチャネル3/4 をリセット同期 PWM モードで起動する。
周期レジスタ \texttt{MTU2TGRA\_3} / \texttt{MTU2TGRC\_3} に
\texttt{\_config.pwmCycle} を設定し、デューティは 0 で開始する。
\end{funcblock}

\begin{funcblock}{updateOutput(sys)}
\texttt{void updateOutput(SystemData\& sys) override}

\vspace{1.5mm}
\texttt{sys.mot.leftCmd} / \texttt{rightCmd} を内部の \texttt{set()} に渡して PWM へ反映し、
クランプ後の値を \texttt{sys.mot.leftActual} / \texttt{rightActual} に書き戻す。
\end{funcblock}

\begin{funcblock}{getLeft() / getRight()}
\texttt{int getLeft() const} / \texttt{int getRight() const}

\vspace{1.5mm}
内部に保持している現在の出力値を返す（デバッグ用）。
\end{funcblock}

\begin{funcblock}{set(left, right) \normalfont\small\color{gray}[private]}
\texttt{void set(int left, int right)}

\vspace{1.5mm}
両輪の値を \texttt{clamp()} で $[-\mathtt{maxPower}, +\mathtt{maxPower}]$ に丸めてから
\texttt{applyLeft()} / \texttt{applyRight()} を呼ぶ。
\end{funcblock}

\begin{funcblock}{applyLeft(val) / applyRight(val) \normalfont\small\color{gray}[private]}
\texttt{void applyLeft(int val)} / \texttt{void applyRight(int val)}

\vspace{1.5mm}
方向ピンを設定し、デューティ値を MTU2 レジスタへ書き込む。
書き込み先は\textbf{バッファレジスタである \texttt{MTU2TGRC\_4}（左）と
\texttt{MTU2TGRD\_4}（右）のみ}で、\texttt{TGRA\_4} / \texttt{TGRB\_4} は
\texttt{init()} 時の初期化にしか使わない。

\vspace{1.5mm}
デューティ値の計算式:
\[
  \mathit{duty} = \frac{(\mathtt{pwmCycle} - 1) \times |\mathit{val}|}{100}
\]
\end{funcblock}

\apisection{ハードウェアマッピング}

\begin{tabularx}{\textwidth}{l l l X}
\toprule
\textbf{信号} & \textbf{ピン} & \textbf{レジスタ} & \textbf{説明} \\
\midrule
左モータ PWM  & P4\_4 (TIOC4A) & MTU2TGRC\_4 & デューティ（バッファレジスタ） \\
右モータ PWM  & P4\_5 (TIOC4B) & MTU2TGRD\_4 & デューティ（バッファレジスタ） \\
左モータ 方向 & P4\_6 (GPIO)   & GPIO.P4 bit6 & \textbf{High:前進 / Low:後退}（極性反転あり） \\
右モータ 方向 & P4\_7 (GPIO)   & GPIO.P4 bit7 & Low:前進 / High:後退 \\
\bottomrule
\end{tabularx}

\begin{cautionbox}[要実機確認: 左モータの方向ピン極性が左右で非対称]
実装 (\texttt{Motor::applyLeft()}) では左モータのみ方向ピンの極性が反転しており、
\textbf{前進時に P4\_6 を High} にする。右モータ (\texttt{applyRight()}) は
前進時に P4\_7 を Low にする通常の極性である。

ソース上のコメントは「左モーターは配線/ギアの都合で正転=後退となっているため、
ソフト側で方向ピンの極性を反転して補正する」と説明しており、
\textbf{機体側の配線に合わせた意図的な補正}と読める。
ただし本仕様書の旧版は左右とも「Low:正転」と記述しており、記述が食い違っている。

本表は\textbf{現在の実装の実態}を記載したものである。
実機で左右モータの回転方向を実測し、
\textbf{(a)} 配線都合の意図的な補正なのか、
\textbf{(b)} 実装側のバグなのかを確定させること。
実測結果によっては \texttt{Motor.cpp} の修正が必要になる。
\end{cautionbox}

\end{apibox}

%% ── 7.6 Servo ──
\subsection{Servo クラス}\label{sec:api-servo}

\begin{apibox}{Servo --- サーボ PWM ドライバ}

\textbf{ファイル:} \texttt{src/drivers/Servo.h}, \texttt{Servo.cpp} \hfill
\textbf{継承:} \texttt{IModule} \hfill
\phasebadge{Output フェーズ}

\vspace{2mm}\hrule\vspace{2mm}

\apisection{概要}

MTU2 チャネル0 を PWM モード1（分周 P0$\phi$/16、周期約 16ms）で駆動し、
ステアリングサーボの角度を制御する。
出力指示は \texttt{sys.srv.angleCmd} に\textbf{整数の角度 [度]}で与える。

\apisection{機能一覧}

\begin{itemize}[nosep]
  \item MTU2 PWM モード1によるサーボ駆動
  \item 角度 $\rightarrow$ パルス幅カウントの変換
  \item \texttt{maxAngle} による舵角クランプ
  \item 反映後の値を \texttt{sys.srv.angleActual} へ書き戻し
\end{itemize}

\apisection{設定 (ServoConfig)}

\begin{tabularx}{\textwidth}{l l X}
\toprule
\textbf{メンバ} & \textbf{実値} & \textbf{説明} \\
\midrule
\texttt{pwmCycle}   & 33332 & PWM 周期カウント値 \\
\texttt{center}     & 3090  & 中心位置のパルス幅カウント値（約 1.5ms） \\
\texttt{handleStep} & 23    & 1度あたりのカウント値 \\
\texttt{maxAngle}   & 40    & 舵角の絶対値上限 [度] \\
\texttt{pwmPinFmt}  & 0     & P4\_0 (TIOC0A) \\
\bottomrule
\end{tabularx}

\apisection{入出力データ (ServoData / sys.srv)}

\begin{tabularx}{\textwidth}{l l l X}
\toprule
\textbf{メンバ} & \textbf{型} & \textbf{方向} & \textbf{説明} \\
\midrule
\texttt{angleCmd}    & \texttt{int} & Logic $\rightarrow$ Servo & 指示角 $[-40, +40]$ [度] \\
\texttt{angleActual} & \texttt{int} & Servo $\rightarrow$ 外部  & クランプ後に実際に反映した角度 \\
\bottomrule
\end{tabularx}

\apisection{関数ドキュメント}

\begin{funcblock}{init()}
\texttt{bool init() override}

\vspace{1.5mm}
P4\_0 を第2代替機能（TIOC0A）に設定し、MTU2 のスタンバイを解除して
チャネル0 を PWM モード1で起動する。
分周は P0$\phi$/16（\texttt{MTU2TCR\_0 = 0x02}）、バッファ転送は TCNT クリア時
（\texttt{MTU2TBTM\_0 = 0x03}）に設定する。
起動時のパルス幅は \texttt{\_config.center}（中央）である。
\end{funcblock}

\begin{funcblock}{updateOutput(sys)}
\texttt{void updateOutput(SystemData\& sys) override}

\vspace{1.5mm}
\texttt{sys.srv.angleCmd} を内部の \texttt{setAngle()} に渡して PWM へ反映し、
クランプ後の角度を \texttt{sys.srv.angleActual} に書き戻す。
\end{funcblock}

\begin{funcblock}{getAngle()}
\texttt{int getAngle() const}

\vspace{1.5mm}
内部に保持している現在の角度を返す（デバッグ用）。
\end{funcblock}

\begin{funcblock}{setAngle(angle) \normalfont\small\color{gray}[private]}
\texttt{void setAngle(int angle)}

\vspace{1.5mm}
角度をクランプしたうえで、バッファレジスタ \texttt{MTU2TGRD\_0} へ書き込む。
変換式は次のとおりで、\textbf{正の角度でカウント値が減少する}。

\[
  \mathtt{MTU2TGRD\_0} = \mathtt{center} - \mathit{angle} \times \mathtt{handleStep}
\]
\end{funcblock}

\apisection{ハードウェアマッピング}

\begin{tabularx}{\textwidth}{l l l X}
\toprule
\textbf{信号} & \textbf{ピン} & \textbf{レジスタ} & \textbf{説明} \\
\midrule
サーボ PWM & P4\_0 (TIOC0A) & MTU2TGRD\_0 & パルス幅（立ち下がりエッジ、バッファレジスタ） \\
\bottomrule
\end{tabularx}

\begin{cautionbox}[要実機確認: 角度の符号と旋回方向の対応]
実装では正の角度がカウント値の\textbf{減少}に対応する
（\texttt{center - angle * handleStep}）。
\texttt{Servo.h} と \texttt{Servo.cpp} のコメントは
「\texttt{angle}: $-\mathtt{maxAngle}$(右) $\sim$ 0(中央) $\sim +\mathtt{maxAngle}$(左)」
「参考プロジェクト \texttt{handle()} に準拠: 正=左」と記述しており、
\textbf{正の角度で左旋回}という前提である。

一方、本仕様書の旧版は $\mathit{val} = \mathit{angle} \times 23 + \mathtt{CENTER}$
（正の角度でカウント値が増加）と記述しており、\textbf{符号が逆}であった。

本節は\textbf{現在の実装の実態}を記載したものである。
実機でサーボに正の角度を与え、車体が実際にどちらへ切れるかを確認し、
\texttt{RunLogic} のクランク/レーンチェンジのハンドル指示（正=左を前提としている）と
整合しているかを検証すること。
食い違っていた場合は左右のクランクが逆方向に曲がるため、走行が成立しない。
\end{cautionbox}

\end{apibox}

%% ── 7.7 Onboard ──
\subsection{Onboard クラス}\label{sec:api-onboard}

\begin{apibox}{Onboard --- オンボード LED / スイッチ ドライバ}

\textbf{ファイル:} \texttt{src/drivers/Onboard.h}, \texttt{Onboard.cpp} \hfill
\textbf{継承:} \texttt{IModule} \hfill
\phasebadge{Input / Output 両フェーズ}

\vspace{2mm}\hrule\vspace{2mm}

\apisection{概要}

GR-PEACH ボード上のフルカラー LED（RGB 3色）+ ユーザー LED（1個）と
ユーザースイッチ（1個）を制御する。
本プロジェクトで唯一、\textbf{Input と Output の両フェーズに登録される}モジュールである。

\begin{itemize}[nosep]
  \item Input フェーズ: \texttt{updateInput()} が P6\_0 を読み \texttt{sys.ob.sw} へ格納
  \item Output フェーズ: \texttt{updateOutput()} が \texttt{sys.ob.*LedCmd} を内部ラッチへ転記し、GPIO へ一括反映
\end{itemize}

\apisection{設定 (OnboardConfig)}

\begin{tabularx}{\textwidth}{l l X}
\toprule
\textbf{メンバ} & \textbf{実値} & \textbf{説明} \\
\midrule
\texttt{swPinFmt}       & 0  & P6\_0 (ユーザースイッチ) \\
\texttt{ledUserPinFmt}  & 12 & P6\_12 (ユーザー LED) \\
\texttt{ledRedPinFmt}   & 13 & P6\_13 (赤) \\
\texttt{ledGreenPinFmt} & 14 & P6\_14 (緑) \\
\texttt{ledBluePinFmt}  & 15 & P6\_15 (青) \\
\bottomrule
\end{tabularx}

\apisection{入出力データ (OnboardData / sys.ob)}

\begin{tabularx}{\textwidth}{l l l X}
\toprule
\textbf{メンバ} & \textbf{型} & \textbf{方向} & \textbf{説明} \\
\midrule
\texttt{sw}           & \texttt{int} & Onboard $\rightarrow$ Logic & 1=押下, 0=解放 \\
\texttt{ledRedCmd}    & \texttt{int} & Logic $\rightarrow$ Onboard & 0=消灯, 1=点灯 \\
\texttt{ledGreenCmd}  & \texttt{int} & Logic $\rightarrow$ Onboard & 同上 \\
\texttt{ledBlueCmd}   & \texttt{int} & Logic $\rightarrow$ Onboard & 同上 \\
\texttt{userLedCmd}   & \texttt{int} & Logic $\rightarrow$ Onboard & 同上 \\
\bottomrule
\end{tabularx}

\apisection{内部変数}

\begin{tabularx}{\textwidth}{l l X}
\toprule
\textbf{変数名} & \textbf{型} & \textbf{説明} \\
\midrule
\texttt{ledState\_[4]} & \texttt{int} & LED ラッチバッファ。添字は 0=赤, 1=緑, 2=青, 3=ユーザー \\
\bottomrule
\end{tabularx}

\apisection{関数ドキュメント}

\begin{funcblock}{init()}
\texttt{bool init() override}

\vspace{1.5mm}
ポート6 の GPIO を初期化する。LED 4本をポートモード（\texttt{PMC6} クリア）・出力
（\texttt{PM6} クリア）・ソフトウェア I/O 制御（\texttt{PIPC6} クリア）に設定し、
全 LED を消灯（High 出力）する。
スイッチ P6\_0 は \texttt{PIPC6} / \texttt{PMC6} をクリアして入力（\texttt{PM6} セット）とし、
\textbf{\texttt{PIBC6} の入力バッファを有効化}する。
\end{funcblock}

\begin{funcblock}{updateInput(sys)}
\texttt{void updateInput(SystemData\& sys) override}

\vspace{1.5mm}
\texttt{sw()} の結果を \texttt{sys.ob.sw} へ格納する。
\end{funcblock}

\begin{funcblock}{updateOutput(sys)}
\texttt{void updateOutput(SystemData\& sys) override}

\vspace{1.5mm}
\texttt{sys.ob.*LedCmd} を内部ラッチ \texttt{ledState\_[]} へ転記した後、
4本の LED をまとめて GPIO へ反映する。
\texttt{ledState\_[i]} が真なら \texttt{GPIO.P6} の該当ビットをクリア（Low 出力＝点灯）、
偽ならセット（High 出力＝消灯）する。
\end{funcblock}

\begin{funcblock}{sw()}
\texttt{int sw()}

\vspace{1.5mm}
ユーザースイッチの状態を即時読み取りで返す。ラッチ方式\textbf{ではなく}、
呼び出し時に \texttt{GPIO.PPR6} を直接読む。
スイッチは active-low のため、読み値が 0 のときに 1（押下）を返す。

\vspace{1.5mm}
\textbf{戻り値:} \texttt{int} --- 1:押下, 0:解放

\vspace{1.5mm}
\texttt{main()} 冒頭の起動モード判定（\hyperref[sec:bootseq]{§\ref*{sec:bootseq}}）で
\texttt{sys.ob.sw} がまだ更新されていない時点から呼べるよう、public のまま残している。
通常の走行ロジックからは \texttt{sys.ob.sw} を読むこと。
\end{funcblock}

\begin{funcblock}{setUserLed(val) / setColorLed(r, g, b) \normalfont\small\color{gray}[private]}
\texttt{void setUserLed(int val)} / \texttt{void setColorLed(int r, int g, int b)}

\vspace{1.5mm}
\texttt{updateOutput()} が内部ラッチを更新するために使う。
外部からは呼べない（旧仕様では public だった）。
LED を操作したい場合は \texttt{sys.ob.*LedCmd} に代入すること。
\end{funcblock}

\apisection{ハードウェアマッピング}

\begin{tabularx}{\textwidth}{l l l X}
\toprule
\textbf{デバイス} & \textbf{ピン} & \textbf{論理} & \textbf{説明} \\
\midrule
LED Red    & P6\_13 & Low Active & 赤色 LED \\
LED Green  & P6\_14 & Low Active & 緑色 LED \\
LED Blue   & P6\_15 & Low Active & 青色 LED \\
LED User   & P6\_12 & Low Active & ユーザー LED（\textbf{要実機確認}、下記参照） \\
SW User    & P6\_0  & Low = 押下 & プルアップ、GND 接続 \\
\bottomrule
\end{tabularx}

\begin{cautionbox}[GPIO 注意事項]
\begin{itemize}[nosep]
  \item スイッチ読み取りには \texttt{PIBC6} の入力バッファ有効化が必須
  \item スイッチ・LED とも \texttt{PIPC6} をクリアしてソフトウェア I/O 制御にすること
        （ペリフェラル I/O 制御のままだと GPIO 操作が効かない）
  \item \texttt{sw()} はラッチ方式\textbf{ではなく}、呼び出し時に直接 GPIO を読む
\end{itemize}
\end{cautionbox}

\begin{cautionbox}[要実機確認: ユーザー LED の論理]
実装 (\texttt{Onboard::updateOutput()}) は RGB 3色とユーザー LED の
\textbf{4本すべてを同一ロジックで扱い}、点灯時に Low を出力する。
\texttt{init()} も4本まとめて High（消灯）で初期化する。
つまり実装上はユーザー LED も \textbf{Low Active} である。

本仕様書の旧版はユーザー LED のみ \textbf{High Active} と記述しており、
「Low Active（RGB）と High Active（ユーザー LED）を考慮して出力する」と説明していた。
実装にはその分岐が存在しない。

本表は\textbf{現在の実装の実態}を記載したものである。
実機で \texttt{sys.ob.userLedCmd = 1} としたときにユーザー LED が
実際に点灯するかを確認し、
\textbf{(a)} 旧仕様の記述が誤りだったのか、
\textbf{(b)} 実装に分岐が必要なのかを確定させること。
\end{cautionbox}

\end{apibox}

%% ── 7.8 Serial ──
\subsection{Serial クラス}\label{sec:api-serial}

\begin{apibox}{Serial --- シリアル通信ドライバ}

\textbf{ファイル:} \texttt{src/drivers/Serial.h}, \texttt{Serial.cpp} \hfill
\textbf{継承:} \texttt{IModule} \hfill
\phasebadge{即時出力（フェーズに依存しない）}

\vspace{2mm}\hrule\vspace{2mm}

\apisection{概要}

SCIF2 を使用してデバッグ用シリアル通信を行う。
他のドライバとは異なりラッチ方式を採用せず、
\texttt{printf()} / \texttt{print()} 呼び出し時に\textbf{即時出力（ポーリング送信）}する。
これにより、途中でクラッシュした場合でもそこまでの出力が確実に端末へ届く。

\apisection{機能一覧}

\begin{itemize}[nosep]
  \item SCIF2 によるシリアル通信（GR-PEACH の DAPLink USB シリアル経由）
  \item 書式付き出力 \texttt{printf()} と固定文字列出力 \texttt{print()}
  \item 即時出力（バッファリングなし、FIFO 空き待ちのポーリング）
\end{itemize}

\apisection{設定 (SerialConfig)}

\begin{tabularx}{\textwidth}{l l X}
\toprule
\textbf{メンバ} & \textbf{実値} & \textbf{説明} \\
\midrule
\texttt{baud}       & \textbf{230400} & ボーレート [bps] \\
\texttt{txPinFmt}   & 3   & P6\_3 (TxD2) \\
\texttt{rxPinFmt}   & 2   & P6\_2 (RxD2) \\
\texttt{bufferSize} & 512 & \texttt{vsnprintf} 用の内部バッファサイズ \\
\bottomrule
\end{tabularx}

\begin{cautionbox}[ターミナルの設定は 230400bps]
旧仕様の 115200bps は誤りである。
端末（TeraTerm 等）を 115200bps に設定すると文字化けする。
ボーレートの導出については \hyperref[sec:scif]{§\ref*{sec:scif}} を参照。
\end{cautionbox}

\apisection{出力データ (SerialData / sys.ser)}

\begin{tabularx}{\textwidth}{l l X}
\toprule
\textbf{メンバ} & \textbf{型} & \textbf{説明} \\
\midrule
\texttt{txCount} & \texttt{uint32\_t} & 送信文字の累計（デバッグ用） \\
\bottomrule
\end{tabularx}

\apisection{内部変数}

\begin{tabularx}{\textwidth}{l l X}
\toprule
\textbf{変数名} & \textbf{型} & \textbf{説明} \\
\midrule
\texttt{buffer\_[512]} & \texttt{char} & \texttt{vsnprintf} の出力先。
  ANSI 色付きの 160 文字行を扱うため 512 バイトに拡大している \\
\bottomrule
\end{tabularx}

\apisection{関数ドキュメント}

\begin{funcblock}{init()}
\texttt{bool init() override}

\vspace{1.5mm}
\texttt{STBCR4} の bit5 をクリアして SCIF2 のモジュールストップを解除し、
P6\_3 / P6\_2 を Alt7（TxD2 / RxD2）に設定してから SCIF2 のレジスタを初期化する。
FIFO リセット $\rightarrow$ ボーレート設定 $\rightarrow$ 1ビット時間以上の待機 $\rightarrow$
FIFO リセット解除 $\rightarrow$ 送受信有効化（\texttt{SCSCR = 0x0030}）の順で行う。
\end{funcblock}

\begin{funcblock}{updateOutput(sys)}
\texttt{void updateOutput(SystemData\& sys) override}

\vspace{1.5mm}
\texttt{IModule} インターフェースを満たすためのダミー実装。何もしない。
出力は \texttt{printf()} / \texttt{print()} の呼び出し時に即座に行われる。
\end{funcblock}

\begin{funcblock}{printf(fmt, ...)}
\texttt{void printf(const char* fmt, ...)}

\vspace{1.5mm}
書式付き文字列を \texttt{vsnprintf} で \texttt{buffer\_} に展開し、
\texttt{putChar()} 経由で1文字ずつ即時送信する。

\vspace{1.5mm}
\textbf{引数:} \texttt{fmt} (\texttt{const char*}) --- 書式文字列（\texttt{printf} 互換）
\end{funcblock}

\begin{funcblock}{print(str)}
\texttt{void print(const char* str)}

\vspace{1.5mm}
書式解析を行わずに文字列をそのまま送信する。
固定文字列や事前に組み立て済みの行バッファを出す場合はこちらのほうが速い。

\vspace{1.5mm}
\textbf{引数:} \texttt{str} (\texttt{const char*}) --- 送信文字列
\end{funcblock}

\begin{funcblock}{putChar(c) \normalfont\small\color{gray}[private]}
\texttt{void putChar(char c)}

\vspace{1.5mm}
送信 FIFO に空きができるまで \texttt{SCFDR} をポーリングしてから
\texttt{SCFTDR} へ1文字書き込み、\texttt{SCFSR} の TDFE / TEND をクリアする。
\end{funcblock}

\apisection{ハードウェアマッピング}

\begin{tabularx}{\textwidth}{l l l X}
\toprule
\textbf{信号} & \textbf{ピン} & \textbf{機能} & \textbf{説明} \\
\midrule
TxD2 & P6\_3 & SCIF2 Alt7 & DAPLink USB シリアルへ物理接続 \\
RxD2 & P6\_2 & SCIF2 Alt7 & 同上 \\
\bottomrule
\end{tabularx}

\begin{notebox}[ピン選択の根拠]
SCIF2 の代替ピンとしては P3\_0 / P3\_2 も存在するが、
\textbf{GR-PEACH の DAPLink 基板には接続されていない}。
mbed の \texttt{PinNames.h} が \texttt{USBTX = P6\_3} と定義しているとおり、
P6\_3 / P6\_2 を使用すること。
\end{notebox}

\end{apibox}

%% ── 7.9 SDCard ──
\subsection{SDCard クラス}\label{sec:api-sdcard}

\begin{apibox}{SDCard --- SD カード SPI ブロックドライバ}

\textbf{ファイル:} \texttt{src/drivers/SDCard.h}, \texttt{SDCard.cpp} \hfill
\textbf{継承:} \texttt{IModule} \hfill
\phasebadge{周期処理なし}

\vspace{2mm}\hrule\vspace{2mm}

\apisection{概要}

RSPI2 を用いた SPI 通信で SD カードにアクセスし、
\textbf{512 バイトのブロック単位}での読み書きを提供する。
本クラスはファイルシステムより下位の層であり、
\textbf{FatFs の \texttt{diskio} 層から呼び出される}。
ログのバッファリングや CSV 整形は
\texttt{SDLogger} (\hyperref[sec:api-sdlogger]{§\ref*{sec:api-sdlogger}}) の役割である。

\begin{notebox}[レイヤ構成]
\texttt{SDLogger}（ログバッファ・CSV 整形）
$\rightarrow$ \texttt{FatFs}（\texttt{f\_open}/\texttt{f\_write}）
$\rightarrow$ \texttt{diskio.cpp}（ブリッジ）
$\rightarrow$ \texttt{SDCard}（ブロック I/O）
$\rightarrow$ RSPI2（SPI）
\end{notebox}

\apisection{機能一覧}

\begin{itemize}[nosep]
  \item RSPI2 による SPI 通信と CS の手動制御
  \item SD カードの初期化シーケンスとカードタイプ判別
  \item 複数ブロックの読み書き（\texttt{readBlocks} / \texttt{writeBlocks}）
  \item カード検出ピンによる挿入判定、CSD からのセクタ数取得
\end{itemize}

\apisection{設定 (SDCardConfig)}

\begin{tabularx}{\textwidth}{l l X}
\toprule
\textbf{メンバ} & \textbf{実値} & \textbf{説明} \\
\midrule
\texttt{csPinFmt}   & 4 & P8\_4 (CS, GPIO 手動制御) \\
\texttt{cdPinFmt}   & 8 & P7\_8 (CD, カード検出) \\
\texttt{mosiPinFmt} & 5 & P8\_5 \\
\texttt{misoPinFmt} & 6 & P8\_6 \\
\texttt{sckPinFmt}  & 3 & P8\_3 \\
\bottomrule
\end{tabularx}

\apisection{カードタイプ (CardType)}

\begin{tabularx}{\textwidth}{l l X}
\toprule
\textbf{列挙子} & \textbf{値} & \textbf{説明} \\
\midrule
\texttt{CARD\_NONE}   & 0 & 未検出 / 初期化失敗 \\
\texttt{CARD\_V1}     & 1 & SD v1.x（標準容量） \\
\texttt{CARD\_V2\_SC} & 2 & SD v2.x 標準容量 \\
\texttt{CARD\_V2\_HC} & 3 & SDHC / SDXC（高容量） \\
\bottomrule
\end{tabularx}

\apisection{内部変数}

\begin{tabularx}{\textwidth}{l l X}
\toprule
\textbf{変数名} & \textbf{型} & \textbf{説明} \\
\midrule
\texttt{initialized\_} & \texttt{bool}      & 初期化成功フラグ \\
\texttt{cardType\_}    & \texttt{CardType}  & 判別したカードタイプ \\
\texttt{sectorCount\_} & \texttt{uint32\_t} & CSD から算出したセクタ数 \\
\bottomrule
\end{tabularx}

\apisection{内部定数}

\begin{tabularx}{\textwidth}{l l X}
\toprule
\textbf{定数名} & \textbf{値} & \textbf{説明} \\
\midrule
\texttt{CMD\_TIMEOUT} & 5000 & コマンド応答待ちのリトライ上限 \\
\bottomrule
\end{tabularx}

\apisection{関数ドキュメント}

\begin{funcblock}{init()}
\texttt{bool init() override}

\vspace{1.5mm}
RSPI2 を初期化し、SD カードの初期化シーケンス
\textbf{CMD0 $\rightarrow$ CMD8 $\rightarrow$ ACMD41 $\rightarrow$ CMD58 $\rightarrow$ CMD16}
を実行する。CMD58 で OCR を読んで SDHC / 標準容量を判別し、
CMD16 でブロック長を 512 バイトに固定する。
成功時に \texttt{initialized\_} を \texttt{true} にする。
\end{funcblock}

\begin{funcblock}{updateOutput(sys)}
\texttt{void updateOutput(SystemData\& sys) override}

\vspace{1.5mm}
未使用。周期処理は行わない。
\end{funcblock}

\begin{funcblock}{readBlocks(buf, block, count)}
\texttt{int readBlocks(uint8\_t* buf, uint32\_t block, uint32\_t count)}

\vspace{1.5mm}
\texttt{block} 番から \texttt{count} ブロック（512 バイト単位）読み出す。

\vspace{1.5mm}
\textbf{戻り値:} \texttt{int} --- 0=成功, 非0=エラー
\end{funcblock}

\begin{funcblock}{writeBlocks(buf, block, count)}
\texttt{int writeBlocks(const uint8\_t* buf, uint32\_t block, uint32\_t count)}

\vspace{1.5mm}
\texttt{block} 番から \texttt{count} ブロック書き込む。

\vspace{1.5mm}
\textbf{戻り値:} \texttt{int} --- 0=成功, 非0=エラー
\end{funcblock}

\begin{funcblock}{isReady() / getCardType() / getSectorCount()}
\texttt{bool isReady() const} / \texttt{CardType getCardType() const} /
\texttt{uint32\_t getSectorCount() const}

\vspace{1.5mm}
初期化状態・カードタイプ・セクタ数をそれぞれ返す。
\end{funcblock}

\begin{funcblock}{主な private メソッド}
\texttt{void initSPI()} / \texttt{void setSPIClock(uint32\_t hz)} /
\texttt{uint8\_t spiTransfer(uint8\_t data)} /
\texttt{void csLow()} / \texttt{void csHigh()} /
\texttt{bool isCardInserted()} /
\texttt{int sendCommand(uint8\_t cmd, uint32\_t arg)} /
\texttt{int sendAppCommand(uint8\_t cmd, uint32\_t arg)} /
\texttt{int readData(uint8\_t* buf, uint32\_t len)} /
\texttt{int writeData(const uint8\_t* buf, uint8\_t token)} /
\texttt{void waitReady()}

\vspace{1.5mm}
\texttt{sendCommand()} は R1 レスポンスを返し、タイムアウト時は $-1$ を返す。
\texttt{waitReady()} は\textbf{戻り値を持たない}（ビジー解除まで待つだけ）。
\texttt{isCardInserted()} は CD ピンが Low のときカードありと判定する。
\end{funcblock}

\apisection{ハードウェアマッピング}

\begin{tabularx}{\textwidth}{l l l X}
\toprule
\textbf{信号} & \textbf{ピン} & \textbf{機能} & \textbf{説明} \\
\midrule
MOSI & P8\_5 & RSPI2 MOSI (Function 3)  & \\
MISO & P8\_6 & RSPI2 MISO (Function 3)  & \\
CLK  & P8\_3 & RSPI2 RSPCK (Function 3) & \\
CS   & P8\_4 & GPIO 出力 & 手動制御 \\
CD   & P7\_8 & GPIO 入力 & カード検出（Low = 挿入） \\
\bottomrule
\end{tabularx}

\end{apibox}

%% ── 7.10 SDLogger ──
\subsection{SDLogger クラス}\label{sec:api-sdlogger}

\begin{apibox}{SDLogger --- 走行ログ記録モジュール}

\textbf{ファイル:} \texttt{src/drivers/SDLogger.h}, \texttt{SDLogger.cpp} \hfill
\textbf{継承:} \texttt{IModule} \hfill
\phasebadge{Output フェーズ（メインループ実行）}

\vspace{2mm}\hrule\vspace{2mm}

\apisection{概要}

走行中のデータを RAM 上のリングバッファに蓄積し、
走行終了後に CSV ファイルとして SD カードへ一括書き出す。
書き込み先は \texttt{/data0000.csv} $\sim$ \texttt{/data9999.csv} で、
連番は \texttt{renban.txt} で管理する。

\begin{cautionbox}[ISR ではなくメインループから呼ぶ]
SD カードのバス処理と FatFs にはタイミング制約があるため、
本モジュールは \texttt{s\_outputModules[]} 配列には登録せず、
メインループから毎周期 \texttt{g\_sdlogger.updateOutput(g\_sys)} を呼ぶ。
\end{cautionbox}

\apisection{機能一覧}

\begin{itemize}[nosep]
  \item 走行中のログ自動記録（パターン番号による記録開始/終了の自動判定）
  \item 4000 エントリ（約 2.7MB）のログバッファ
  \item 各エントリに画像1行分（160 画素）の生データを同梱
  \item CSV 形式でのバッチ書き出しと連番ファイル管理
\end{itemize}

\apisection{設定 (SDLoggerConfig)}

\begin{tabularx}{\textwidth}{l l X}
\toprule
\textbf{メンバ} & \textbf{実値} & \textbf{説明} \\
\midrule
\texttt{maxEntries}       & 4000 & バッファのエントリ数上限（配列上限は \texttt{SDLOG\_MAX\_ENTRIES}） \\
\texttt{imageWidth}       & 160  & 画像1行記録時の幅 \\
\texttt{recordImageRow}   & 45   & 画像を記録する行番号。偏差の記録行も兼ねる \\
\texttt{patternMinForLog} & 11   & この番号以上のパターンで記録を開始（\texttt{RP\_TRACE\_NORMAL}） \\
\texttt{finishPattern}    & 101  & このパターンは記録対象外（\texttt{RP\_FINISH}） \\
\bottomrule
\end{tabularx}

\apisection{入出力データ (SDLoggerData / sys.sd)}

\begin{tabularx}{\textwidth}{l l l X}
\toprule
\textbf{メンバ} & \textbf{型} & \textbf{方向} & \textbf{説明} \\
\midrule
\texttt{ready}         & \texttt{bool}      & SDLogger $\rightarrow$ 外部 & FatFs マウント成功フラグ \\
\texttt{logCount}      & \texttt{uint32\_t} & SDLogger $\rightarrow$ 外部 & 現在の記録エントリ数 \\
\texttt{full}          & \texttt{bool}      & SDLogger $\rightarrow$ 外部 & バッファ満杯フラグ \\
\texttt{saveRequested} & \texttt{bool}      & 外部 $\rightarrow$ SDLogger & \texttt{true} で書き出しを要求 \\
\texttt{saveDone}      & \texttt{bool}      & SDLogger $\rightarrow$ 外部 & 書き出し完了フラグ \\
\bottomrule
\end{tabularx}

\apisection{ログエントリ (SDLOG\_T)}

\begin{tabularx}{\textwidth}{l l X}
\toprule
\textbf{メンバ} & \textbf{型} & \textbf{記録元} \\
\midrule
\texttt{cnt\_msdwritetime} & \texttt{unsigned int} & \texttt{g\_timer\_1ms}（タイムスタンプ） \\
\texttt{pattern}    & \texttt{unsigned int} & \texttt{sys.run.pattern} \\
\texttt{convertBCD} & \texttt{unsigned int} & \texttt{sys.line.sensorBin} \\
\texttt{handle}     & \texttt{signed int}   & \texttt{sys.run.handleVal} \\
\texttt{hennsa}     & \texttt{signed int}   & \texttt{sys.line.deviation[recordImageRow]} \\
\texttt{encoder}    & \texttt{unsigned int} & \texttt{sys.enc.totalCount} \\
\texttt{motorL / motorR} & \texttt{signed int} & \texttt{sys.mot.leftActual / rightActual} \\
\texttt{flagL / flagK / flagR / flagS} & \texttt{signed int} &
  \texttt{sys.line.leftLine / crossLine / rightLine / centerLine} \\
\texttt{total}      & \texttt{signed int}   & \texttt{g\_timer\_1ms} \\
\texttt{imageData0[160]} & \texttt{signed int} & \texttt{sys.cam.imageBufferPtr} の \texttt{recordImageRow} 行 \\
\bottomrule
\end{tabularx}

\apisection{関数ドキュメント}

\begin{funcblock}{init()}
\texttt{bool init() override}

\vspace{1.5mm}
\texttt{SDCard} を初期化して FatFs をマウントし、ログバッファをリセットする。
成功時に \texttt{mounted\_} を \texttt{true} にする。
\end{funcblock}

\begin{funcblock}{updateOutput(sys)}
\texttt{void updateOutput(SystemData\& sys) override}

\vspace{1.5mm}
次の3つを毎周期実行する。

\begin{enumerate}[nosep, leftmargin=*]
  \item \texttt{sys.sd.ready} / \texttt{logCount} / \texttt{full} を現在値で更新
  \item \texttt{sys.run.pattern} が \texttt{patternMinForLog} 以上かつ
        \texttt{finishPattern} 以外なら、\texttt{SystemData} から1エントリ分を収集して \texttt{commit()}
  \item \texttt{sys.sd.saveRequested} が立っていれば \texttt{saveToSD()} を実行し、
        成功時に \texttt{sys.sd.saveDone} を立てる
\end{enumerate}
\end{funcblock}

\begin{funcblock}{saveToSD()}
\texttt{int saveToSD()}

\vspace{1.5mm}
バッファ内の全エントリを CSV ファイルとして SD カードへ書き出す。
\texttt{renban.txt} から連番を読み、\texttt{/dataNNNN.csv} を作成して書き込み、連番を更新する。

\vspace{1.5mm}
\textbf{戻り値:} \texttt{int} --- 0=成功, 非0=エラー
\end{funcblock}

\begin{funcblock}{current() / commit()}
\texttt{SDLOG\_T\& current()} / \texttt{void commit()}

\vspace{1.5mm}
\texttt{current()} は次に書き込むエントリへの参照を返し、
\texttt{commit()} が記録番地を1つ進める。
バッファが満杯の場合、\texttt{current()} は最終エントリを返して上書きを防ぐ。
\end{funcblock}

\begin{funcblock}{isReady() / getLogCount() / isFull() / resetLog()}
\texttt{bool isReady() const} / \texttt{unsigned int getLogCount() const} /
\texttt{bool isFull() const} / \texttt{void resetLog()}

\vspace{1.5mm}
マウント状態・記録数・満杯判定の取得と、バッファのリセット。
\end{funcblock}

\apisection{使用例}

\begin{lstlisting}[caption={SDLogger の組み込み例（メインループ）},label=lst:sdlogger]
// main 起動時（GIC 初期化の後、Camera 初期化の前）
g_sdlogger.init();

// メインループ内
g_sdlogger.updateOutput(g_sys);   // 走行中ログの自動記録

if (g_sys.run.finished && !savedToSD) {
    savedToSD = true;
    g_sys.mot.leftCmd  = 0;
    g_sys.mot.rightCmd = 0;
    g_sys.srv.angleCmd = 0;
    g_sys.sd.saveRequested = true;
    g_sdlogger.updateOutput(g_sys);  // 即座に保存実行
}
\end{lstlisting}

\end{apibox}

%% ── 7.11 RunLogic ──
\subsection{RunLogic --- 走行ロジック}\label{sec:api-runlogic}

\begin{apibox}{RunLogic --- 走行状態機械（Logic レイヤー）}

\textbf{ファイル:} \texttt{src/logic/RunLogic.h}, \texttt{RunLogic.cpp} \hfill
\textbf{種別:} \textbf{自由関数の集合（クラスではない）} \hfill
\phasebadge{Logic フェーズ}

\vspace{2mm}\hrule\vspace{2mm}

\apisection{概要}

参考プロジェクト \texttt{mcr\_shiozawa\_cclass\_2.38m-s/main.cpp} の
\texttt{intTimer()} 内 \texttt{switch(pattern)} 状態遷移を移植したもの。
旧設計ではこれを \texttt{RunController} というクラスにしていたが、
EMA 化にあたり\textbf{クラスを廃して純関数 \texttt{applyDrivingPattern()} に分解}し、
インスタンス変数はすべて \texttt{SystemData::run}（\texttt{RunData}）へ移した。

\begin{itemize}[nosep]
  \item 入力: \texttt{sys.line.*}（ライン検出結果）、\texttt{sys.ob.sw}（SW 状態）、\texttt{sys.run.*}（自身の状態）
  \item 出力: \texttt{sys.mot.*Cmd}, \texttt{sys.srv.angleCmd}, \texttt{sys.ob.*LedCmd}, \texttt{sys.run.*}
  \item \textbf{ハードウェアを直接呼ばない}（\texttt{g\_motor} / \texttt{g\_servo} / \texttt{g\_onboard} を参照しない）
  \item 全パラメータは \texttt{RUN\_CONFIG} / \texttt{MOTOR\_CONFIG} から取得する
\end{itemize}

オリジナルは「エンコーダ累積パルス」で距離を管理していたが、
本実装では読みやすさを優先してすべて「経過時間 [ms]」で管理する。
時間管理は \texttt{ModuleTimer} (\hyperref[sec:moduletimer]{§\ref*{sec:moduletimer}}) が担う。

\apisection{状態データ (RunData / sys.run)}

\begin{tabularx}{\textwidth}{l l X}
\toprule
\textbf{メンバ} & \textbf{型} & \textbf{説明} \\
\midrule
\texttt{pattern}       & \texttt{int}  & 現在の走行パターン番号 \\
\texttt{prevPattern}   & \texttt{int}  & 直前のパターン（遷移検知用） \\
\texttt{handleVal}     & \texttt{int}  & 直近のハンドル指示値 [度] \\
\texttt{traceOffset}   & \texttt{int}  & ハンドル計算のオフセット \\
\texttt{leftMotor} / \texttt{rightMotor} & \texttt{int} & \texttt{calcMotorVal()} の結果 \\
\texttt{leftBrake} / \texttt{rightBrake} & \texttt{int} & \texttt{calcBrakeMotorVal()} の結果 \\
\texttt{lastHandle101} & \texttt{int}  & FINISH 時に維持するハンドル値 \\
\texttt{finished}      & \texttt{bool} & 走行終了フラグ。メインループが SD 保存のトリガに使う \\
\texttt{stateTimer}    & \texttt{ModuleTimer} & 現パターンに入ってからの経過時間（旧 \texttt{stateMs\_}） \\
\texttt{totalTimer}    & \texttt{ModuleTimer} & 走行スタートからの経過時間（旧 \texttt{totalMs\_}） \\
\texttt{auxTimer}      & \texttt{ModuleTimer} & 補助タイマー（バー検知等、旧 \texttt{cnt1Ms\_}） \\
\bottomrule
\end{tabularx}

\apisection{状態遷移（RunPattern）}

\begin{longtable}{l l l}
\toprule
\textbf{番号} & \textbf{列挙子} & \textbf{役割} \\
\midrule
\endfirsthead
\toprule
\textbf{番号} & \textbf{列挙子} & \textbf{役割} \\
\midrule
\endhead
0   & \texttt{RP\_WAIT\_SW}             & スイッチ入力待ち \\
1   & \texttt{RP\_APPROACH\_BAR}        & スタートバーへ前進 \\
2   & \texttt{RP\_WAIT\_AT\_BAR}        & バー手前で停止 \\
3   & \texttt{RP\_ACCEL\_AFTER\_BAR}    & バー上昇後の加速 \\
4   & \texttt{RP\_CHECK\_BAR\_OPEN}     & バー開放確認 \\
10  & \texttt{RP\_WAIT\_BAR\_OPEN}      & バー開放待ち \\
11  & \texttt{RP\_TRACE\_NORMAL}        & 通常トレース \\
21  & \texttt{RP\_CROSSLINE\_DETECTED}  & クロスライン検出 \\
22  & \texttt{RP\_CROSSLINE\_SKIP}      & クロスラインの読み飛ばし \\
23  & \texttt{RP\_CROSSLINE\_AFTER}     & クロスライン後のトレース・クランク検出 \\
30  & \texttt{RP\_LEFT\_CRANK\_ENTRY}   & 左クランク開始 \\
31  & \texttt{RP\_LEFT\_CRANK}          & 左クランク中 \\
40  & \texttt{RP\_RIGHT\_CRANK\_ENTRY}  & 右クランク開始 \\
41  & \texttt{RP\_RIGHT\_CRANK}         & 右クランク中 \\
51  & \texttt{RP\_RIGHT\_HALF\_DETECTED}& 右ハーフライン検出 \\
52  & \texttt{RP\_RIGHT\_HALF\_SKIP}    & 右ハーフラインの読み飛ばし \\
53  & \texttt{RP\_RIGHT\_HALF\_AFTER}   & 右ハーフライン後のトレース \\
54  & \texttt{RP\_RIGHT\_LANE\_PREPARE} & 右レーンチェンジ準備 \\
55  & \texttt{RP\_RIGHT\_LANE\_TURN}    & 右レーンチェンジ：寄せ \\
56  & \texttt{RP\_RIGHT\_LANE\_STRAIGHT}& 右レーンチェンジ：直進 \\
57  & \texttt{RP\_RIGHT\_LANE\_COUNTER} & 右レーンチェンジ：戻し \\
61  & \texttt{RP\_LEFT\_HALF\_DETECTED} & 左ハーフライン検出 \\
62  & \texttt{RP\_LEFT\_HALF\_SKIP}     & 左ハーフラインの読み飛ばし \\
63  & \texttt{RP\_LEFT\_HALF\_AFTER}    & 左ハーフライン後のトレース \\
64  & \texttt{RP\_LEFT\_LANE\_PREPARE}  & 左レーンチェンジ準備 \\
65  & \texttt{RP\_LEFT\_LANE\_TURN}     & 左レーンチェンジ：寄せ \\
66  & \texttt{RP\_LEFT\_LANE\_STRAIGHT} & 左レーンチェンジ：直進 \\
67  & \texttt{RP\_LEFT\_LANE\_COUNTER}  & 左レーンチェンジ：戻し \\
101 & \texttt{RP\_FINISH}               & 走行終了（ログ保存待ち） \\
\bottomrule
\end{longtable}

\begin{notebox}[パターン番号は他モジュールからも参照される]
\texttt{LineDetector} は \texttt{pattern == 11} / \texttt{1} / \texttt{2} で
検出パラメータを切り替え、\texttt{SDLogger} は
\texttt{patternMinForLog = 11} 以上で記録を開始し \texttt{finishPattern = 101} を除外する。
番号を変更する場合は \texttt{ProjectConfig.h} 側の対応値も合わせること。
\end{notebox}

\apisection{関数ドキュメント}

\begin{funcblock}{runLogicInit(sys)}
\texttt{void runLogicInit(SystemData\& sys)}

\vspace{1.5mm}
起動時に1回だけ呼ぶ初期化。\texttt{sys.run.*} をすべて初期値へ戻し、
\texttt{pattern} を \texttt{RP\_WAIT\_SW} に設定して3本の \texttt{ModuleTimer} を起動する。
あわせて初期出力指示（\texttt{sys.mot.*Cmd}, \texttt{sys.srv.angleCmd},
\texttt{sys.ob.*LedCmd}）をすべて 0 にクリアする。
\end{funcblock}

\begin{funcblock}{applyDrivingPattern(sys)}
\texttt{void applyDrivingPattern(SystemData\& sys)}

\vspace{1.5mm}
Logic フェーズで毎周期呼ぶ本体。処理の順序は次のとおり。

\begin{enumerate}[nosep, leftmargin=*]
  \item \texttt{sys.ob.userLedCmd} を中央2点センサ（\texttt{sensorBin \& 0x18}）の反応で更新
  \item \texttt{calcHandle(sys)} でハンドル指示値を算出
  \item \texttt{calcMotorVal(sys)} で通常走行時のモータ値を算出
  \item \texttt{switch (sys.run.pattern)} で29個の \texttt{runXxx()} 関数へ分岐
        （未定義の番号は \texttt{RP\_WAIT\_SW} へ戻す）
  \item \texttt{sys.run.prevPattern} を更新
\end{enumerate}
\end{funcblock}

\apisection{ハンドル指示値の計算}

\texttt{calcHandle()}（\texttt{RunLogic.cpp} 内の static 関数）は
PID ではなく\textbf{比例ゲインの3段切替}でハンドル角を決める。
本プロジェクトで最も頻繁に調整するロジックである。

\vspace{2mm}
$\mathit{dev} = \mathtt{sys.line.deviation[traceRow]}$、
$\mathit{corrected} = \mathit{dev} + \mathtt{sys.run.traceOffset}$ として:

\begin{tabularx}{\textwidth}{l X}
\toprule
\textbf{条件} & \textbf{ハンドル指示値} \\
\midrule
\texttt{pattern < RP\_TRACE\_NORMAL} & 更新しない（停止系のため直前値を維持） \\
\texttt{pattern != RP\_TRACE\_NORMAL} & $\mathit{corrected} \times \mathtt{handleGainNonNormal}$ \\
$|\mathit{dev}| \leq \mathtt{handleStraightAbsThreshold}$ & 0（直線とみなす） \\
$|\mathit{dev}| \leq \mathtt{handleGentleAbsThreshold}$   & $\mathit{corrected} \times \mathtt{handleGainGentle}$（緩カーブ） \\
上記以外 & $\mathit{corrected} \times \mathtt{handleGainSharp}$（急カーブ） \\
\bottomrule
\end{tabularx}

\vspace{2mm}
通常走行時のモータ出力は \texttt{calcMotorVal()} が左右とも
\texttt{MOTOR\_CONFIG.maxPower} に設定する。
ブレーキ時は \texttt{calcBrakeMotorVal()} が \texttt{RUN\_CONFIG.brakeTargetMotor} を用いる。

\apisection{距離 $\rightarrow$ タイマー置換の対応表}

参考プロジェクト（エンコーダ距離）$\rightarrow$ 本実装（経過時間 ms）の対応:

\begin{tabularx}{\textwidth}{l l X}
\toprule
\textbf{参考プロジェクト} & \textbf{本実装} & \textbf{備考} \\
\midrule
\texttt{encoder.clear()} & \texttt{sys.run.stateTimer.setTime()} & \texttt{changePattern()} が状態遷移時に自動実行 \\
\texttt{encoder.getTotalCount() >= N} & \texttt{sys.run.stateTimer.isExpired(N)} & 1 pulse $\approx$ 1ms 換算 \\
\texttt{encoder.getCourseCount() >= ONEM*58} & \texttt{sys.run.totalTimer.isExpired(tCourseTimeoutMs)} & 既定 120 秒 \\
\texttt{encoder.getCnt() <= lowSpeedLimit} & （削除） & 速度判定はカット \\
\bottomrule
\end{tabularx}

\apisection{使用例}

\begin{lstlisting}[caption={RunLogic の組み込み例},label=lst:runlogic]
// main 起動時（全ドライバの init() 完了後、initOSTM0() の前）
runLogicInit(g_sys);

// OSTM0 1ms 割り込みコールバック内 — Logic フェーズ
if (!s_debugMode) {
    applyDrivingPattern(g_sys);   // 状態遷移を 1ms 毎に進める
}

// メインループ — 走行終了の検知
if (g_sys.run.finished && !savedToSD) {
    savedToSD = true;
    g_sys.sd.saveRequested = true;
    g_sdlogger.updateOutput(g_sys);
}
\end{lstlisting}

\begin{notebox}[旧 RunController からの移行対応]
旧仕様書に記載されていた API は次のように置き換わっている。

\begin{tabularx}{\textwidth}{l X}
\toprule
\textbf{旧 API} & \textbf{現在の参照方法} \\
\midrule
\texttt{g\_runController.init()}      & \texttt{runLogicInit(g\_sys)} \\
\texttt{g\_runController.update()}    & \texttt{applyDrivingPattern(g\_sys)} \\
\texttt{isFinished()}                 & \texttt{g\_sys.run.finished} を直読 \\
\texttt{getPattern()}                 & \texttt{g\_sys.run.pattern} を直読 \\
\texttt{getHandleVal()}               & \texttt{g\_sys.run.handleVal} を直読 \\
\texttt{forceStop()}                  & 該当なし。\texttt{g\_sys.run.pattern = RP\_FINISH} 等で代替 \\
\bottomrule
\end{tabularx}
\end{notebox}

\end{apibox}



%% ============================================================
%% 8. 実行モデル詳細
%% ============================================================
\section{実行モデル詳細}\label{sec:execution}

\subsection{割り込みハンドラ}

割り込みハンドラ関数内で各モジュールの \texttt{update()} を呼び出す。
\texttt{Pattern} 処理はクラス化せず、ハンドラ内にベタ書き（または関数化）する。

\begin{lstlisting}[caption={割り込みハンドラ実装例},label=lst:handler]
// グローバルインスタンス
static Onboard onboard;
static Motor motor;
static Servo servo;

void interrupt_handler() {
    // --- Input Phase ---
    // encoder.read();
    // camera.read();

    // --- Logic Phase (Pattern) ---
    int sw = onboard.sw();
    if (sw) {
        motor.setPower(0.2f, 0.2f);  // PWM 20%
        servo.setAngle(10.0f);       // 10度
        onboard.setLed(0, 1);        // Red ON
        onboard.setLed(1, 1);        // Green ON
        onboard.setLed(2, 1);        // Blue ON
    } else {
        motor.setPower(0.0f, 0.0f);
        servo.setAngle(0.0f);
        onboard.setLed(0, 1);        // Red ON
        onboard.setLed(1, 0);        // Green OFF
        onboard.setLed(2, 0);        // Blue OFF
    }

    // --- Output Phase ---
    motor.update();
    servo.update();
    onboard.update();
    // serial.update();
    // sd.update();
}
\end{lstlisting}

\subsection{ラッチ方式ドライバの動作原理}

\begin{figure}[H]
\centering
\begin{tikzpicture}[
  box/.style={draw=headerblue, fill=headerblue!8, rounded corners=2pt,
              minimum width=32mm, minimum height=12mm, font=\small, align=center},
  data/.style={draw=accentblue, fill=accentblue!10, rounded corners=2pt,
               minimum width=25mm, minimum height=9mm, font=\small\ttfamily, align=center},
  arr/.style={-{Stealth[length=3mm]}, thick, color=accentblue},
  lbl/.style={font=\scriptsize\color{gray}, above, midway},
]

\node[box] (logic) {Logic Phase\\(Pattern)};
\node[data, right=15mm of logic] (var) {\_pwmLeft\\\_pwmRight};
\node[box, right=15mm of var] (update) {update()};
\node[data, right=15mm of update] (hw) {MTU2\\レジスタ};

\draw[arr] (logic) -- node[lbl]{setPower()} (var);
\draw[arr] (var) -- node[lbl]{参照} (update);
\draw[arr] (update) -- node[lbl]{書込み} (hw);

\end{tikzpicture}
\caption{ラッチ方式ドライバのデータフロー}
\label{fig:latch}
\end{figure}

\end{document}
